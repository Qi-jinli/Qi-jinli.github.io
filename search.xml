<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>常用方法总结</title>
      <link href="2021/01/31/%E5%B8%B8%E7%94%A8%E6%96%B9%E6%B3%95%E6%80%BB%E7%BB%93/"/>
      <url>2021/01/31/%E5%B8%B8%E7%94%A8%E6%96%B9%E6%B3%95%E6%80%BB%E7%BB%93/</url>
      
        <content type="html"><![CDATA[<h1 id="Numpy"><a href="#Numpy" class="headerlink" title="Numpy"></a>Numpy</h1><h2 id="创建"><a href="#创建" class="headerlink" title="创建"></a>创建</h2><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs python">numpy.array(<span class="hljs-built_in">object</span>,dtype=<span class="hljs-literal">None</span>,copy=<span class="hljs-literal">True</span>,order=<span class="hljs-literal">None</span>,subok=<span class="hljs-literal">False</span>,ndmin=<span class="hljs-number">0</span>) <span class="hljs-comment"># 列表或元组转换</span><br>numpy.arange(start,stop,step,dtype=<span class="hljs-literal">None</span>) <span class="hljs-comment"># 在给定区间内创建一系列均匀间隔的值，指定[开始，停止),并指定步长step</span><br>numpy.linspace(start, stop, num=<span class="hljs-number">50</span>, endpoint=<span class="hljs-literal">True</span>, retstep=<span class="hljs-literal">False</span>, dtype=<span class="hljs-literal">None</span>) <span class="hljs-comment"># 同arange一样</span><br>numpy.logspace(start, stop, num) <span class="hljs-comment"># 以10为底，生成num个等比数列</span><br>numpy.ones(shape, dtype=<span class="hljs-literal">None</span>, order=<span class="hljs-string">&#x27;C&#x27;</span>) <span class="hljs-comment"># 生成全为1的多维数组</span><br>numpy.ones_like(a, dtype) <span class="hljs-comment"># 快速创建数值全部为1的多维数组</span><br>numpy.zeros(shape, dtype=<span class="hljs-literal">None</span>, order=<span class="hljs-string">&#x27;C&#x27;</span>) <span class="hljs-comment"># 生成全为0的多维数组</span><br>numpy.zeros_like(a, dtype) <span class="hljs-comment"># 快速创建数值全部为0的多维数组</span><br>numpy.eye(N, M=<span class="hljs-literal">None</span>, k=<span class="hljs-number">0</span>, dtype=&lt;<span class="hljs-built_in">type</span> <span class="hljs-string">&#x27;float&#x27;</span>&gt;)  <span class="hljs-comment"># 用于创建一个二维数组，特点是k对角线上的值为1，其余值全部为0</span><br>numpy.random.rand(d0, d1, ..., dn )<span class="hljs-comment"># 方法的作用为：指定一个数组，并使用[0, 1)区间随机数据填充，这些数据均匀分布。</span><br>numpy.random.randn(d0, d1, ..., dn) <span class="hljs-comment"># 从标准正态分布中返回一个或多个样本值</span><br>numpy.random.standard_normal(size) <span class="hljs-comment"># 从标准正态分布中生成随机数</span><br>numpy.random.randint(low, high, size, dtype) <span class="hljs-comment"># 方法将会生成[low, high)的随机整数</span><br>numpy.random.normal(loc，scale，size) <span class="hljs-comment"># 从正态分布绘制随机样本。参数，均值和偏差</span><br>numpy.random.uniform(low=<span class="hljs-number">0</span>，high=<span class="hljs-number">1</span>，size) <span class="hljs-comment"># 从均匀分布中生成随机数</span><br></code></pre></td></tr></table></figure></blockquote><h2 id="属性"><a href="#属性" class="headerlink" title="属性"></a>属性</h2><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs python">ndarray.T <span class="hljs-comment"># 用于数组的转置，与.transpose()相同。</span><br>ndarray.dtype <span class="hljs-comment"># 用来输出数组包含元素的数据类型。</span><br>ndarray.real <span class="hljs-comment"># 用来输出数组包含元素的实部。</span><br>ndarray.imag <span class="hljs-comment"># 用来输出数组包含元素的虚部。</span><br>ndarray.size <span class="hljs-comment"># 用来输出数组中的总包含元素数。</span><br>ndarray.itemsize <span class="hljs-comment"># 输出一个数组元素的字节数。</span><br>ndarray.nbytes <span class="hljs-comment"># 用来输出数组的元素总字节数。</span><br>ndarray.ndim <span class="hljs-comment"># 用来输出数组维度。</span><br>ndarray.shape <span class="hljs-comment"># 用来输出数组形状。</span><br>ndarray.strides <span class="hljs-comment"># 用来遍历数组时，输出每个维度中步进的字节数组。</span><br></code></pre></td></tr></table></figure></blockquote><h2 id="数组操作"><a href="#数组操作" class="headerlink" title="数组操作"></a>数组操作</h2><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs python">ndarray.reshape(newshape) <span class="hljs-comment"># 可以在不改变数组数据的同时改变数组形状</span><br>ndarray.resize(newshape) <span class="hljs-comment"># 改变数组形状，改变数组数据，新数组比原数组大，则将会copy原数组中的值对新数组进行填充</span><br>ndarray.ravel(order=<span class="hljs-string">&#x27;C&#x27;</span>) <span class="hljs-comment"># 将任意形状的按行读取，变为1维数组，浅拷贝</span><br>ndarray.flatten() <span class="hljs-comment"># 将任意形状的按行读取，变为1维数组，深拷贝</span><br>numpy.moveaxis(a, source, destination) <span class="hljs-comment"># 将数组的轴移动到新的位置</span><br>numpy.swapaxes(a, axis1, axis2) <span class="hljs-comment"># 用来交换数组的轴</span><br>numpy.concatenate((a1, a2, ...), axis=<span class="hljs-number">0</span>) <span class="hljs-comment"># 可以将多个数组沿指定轴连接在一起</span><br>numpy.unique(a) <span class="hljs-comment"># 去重数组元素中重复的元素</span><br>numpy.atleast_xd() <span class="hljs-comment"># 支持输入数据直接视为x维</span><br></code></pre></td></tr></table></figure></blockquote><h2 id="逻辑与统计计算"><a href="#逻辑与统计计算" class="headerlink" title="逻辑与统计计算"></a>逻辑与统计计算</h2><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs python">ndarray[条件] = <span class="hljs-number">1</span> <span class="hljs-comment"># 将满足条件的数组元素设置为1</span><br>numpy.<span class="hljs-built_in">all</span>(条件) <span class="hljs-comment"># 查询是否都满足条件</span><br>numpy.<span class="hljs-built_in">any</span>(条件) <span class="hljs-comment"># 查询是否有满足条件的</span><br>numpy.where(条件，<span class="hljs-number">1</span>，<span class="hljs-number">0</span>) <span class="hljs-comment"># 三目运算，符合条件的设置为1，不符合的为0</span><br>numpy.<span class="hljs-built_in">max</span>() <span class="hljs-comment"># 沿指定轴计算中间值</span><br>numpy.<span class="hljs-built_in">min</span>() <span class="hljs-comment"># 沿指定轴计算中间值</span><br>numpy.median() <span class="hljs-comment"># 沿指定轴计算中间值</span><br>numpy.mean() <span class="hljs-comment"># 沿指定轴计算平均数</span><br>numpy.std() <span class="hljs-comment"># 沿指定轴计算标准偏差</span><br>numpy.var() <span class="hljs-comment"># 沿指定轴计算方差</span><br>numpy.argmax(axis=) <span class="hljs-comment"># 最大元素对应的下标</span><br>numpy.argmin(axis=) <span class="hljs-comment"># 最小元素对应的下标</span><br>numpy.dot(a, b) <span class="hljs-comment"># 求解两个数组的点积</span><br>numpy.matmul(a, b) <span class="hljs-comment"># 求解两个数组的矩阵乘积</span><br></code></pre></td></tr></table></figure></blockquote><h1 id="Pandas"><a href="#Pandas" class="headerlink" title="Pandas"></a>Pandas</h1><h2 id="创建-1"><a href="#创建-1" class="headerlink" title="创建"></a>创建</h2><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python">pandas.Series(data=<span class="hljs-literal">None</span>,index=<span class="hljs-literal">None</span>) <span class="hljs-comment"># 创建一位数组，data可以是字典，或者Numpy中的ndarray，index是索引</span><br>pandas.DataFrame(data=<span class="hljs-literal">None</span>, index=<span class="hljs-literal">None</span>, columns=<span class="hljs-literal">None</span>) <span class="hljs-comment"># 创建二维数组，data为数组、列表、字典，或者ndarray</span><br>pandas.MultiIndex.from_arrays(arrays, names=(<span class="hljs-string">&#x27;number&#x27;</span>, <span class="hljs-string">&#x27;color&#x27;</span>)) <span class="hljs-comment"># 创建三维数组</span><br></code></pre></td></tr></table></figure></blockquote><h2 id="属性-1"><a href="#属性-1" class="headerlink" title="属性"></a>属性</h2><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs python">Series.index <span class="hljs-comment"># 查看一维数组中的索引</span><br>Series.values <span class="hljs-comment"># 查看一位数组中的值</span><br>DataFrame.head() <span class="hljs-comment"># 默认显示前5条</span><br>DataFrame.tail(<span class="hljs-number">7</span>) <span class="hljs-comment"># 指定显示后7条</span><br>DataFrame.describe()<span class="hljs-comment"># 对数据集进行概览，会输出每一列数据的计数、最大值、最小值</span><br>DataFrame.values <span class="hljs-comment"># 将DataFrame转换成Numpy数组</span><br>DataFrame.index <span class="hljs-comment"># 查看索引</span><br>DataFrame.columns <span class="hljs-comment"># 查看列名</span><br>DataFrame.shape <span class="hljs-comment"># 查看形状</span><br>MultiIndex.index <span class="hljs-comment"># 查看multiIndex对象</span><br>MultiIndex.index.names <span class="hljs-comment"># 查看levels的名称</span><br>MultiIndex.index.levels <span class="hljs-comment"># 查看每个level的值</span><br></code></pre></td></tr></table></figure></blockquote><h2 id="数据操作"><a href="#数据操作" class="headerlink" title="数据操作"></a>数据操作</h2><h3 id="索引"><a href="#索引" class="headerlink" title="索引"></a>索引</h3><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs python">DataFrame.iloc[[行],[列]] <span class="hljs-comment"># 基于数字索引对数据集进行选择，支持切片(前闭后开)，列表</span><br>DataFrame.loc[[行],[列]] <span class="hljs-comment"># 基于索引名对数据集进行选择，支持切片(首尾包含)，列表</span><br>DataFrame.ix[[行],[列]] <span class="hljs-comment"># 基于下标和名称组合索引</span><br>DataFrame[列索引值][行索引值] <span class="hljs-comment"># 先列后行，按照索引的字符串进行索引</span><br>DataFrame.index = 新索引值 <span class="hljs-comment"># 修改行列索引值</span><br>DataFrame.reset_index(drop=<span class="hljs-literal">False</span>) <span class="hljs-comment"># 设置新的下标索引,默认为False，不删除原来索引</span><br>DataFrame.set_index(keys, drop=<span class="hljs-literal">True</span>) <span class="hljs-comment"># 设置以keys为列索引，默认为True，删除原来索引</span><br>DataFrame[列名]=<span class="hljs-number">1</span> 或 DataFrame.列名=<span class="hljs-number">1</span> <span class="hljs-comment"># 将一列赋值为1</span><br></code></pre></td></tr></table></figure></blockquote><h3 id="排序"><a href="#排序" class="headerlink" title="排序"></a>排序</h3><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs python">Series.sort_values(ascending=<span class="hljs-literal">True</span>) <span class="hljs-comment"># 对数据进行排序，默认升序</span><br>Series.sort_index() <span class="hljs-comment"># 对索引进行排序</span><br>DataFrame.sort_values(by=, ascending=) <span class="hljs-comment"># 按照一个键或多个进行数据排序，默认升序</span><br>DataFrame.sort_index(axis=) <span class="hljs-comment"># 对指定轴索引进行排序，从小到大</span><br></code></pre></td></tr></table></figure></blockquote><h3 id="数据删减"><a href="#数据删减" class="headerlink" title="数据删减"></a>数据删减</h3><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs python">DataFrame.drop(labels=[], axis=) <span class="hljs-comment"># 按照指定轴对指定标签参数进行删除</span><br>DataFrame.drop_duplicates(axis=) <span class="hljs-comment"># 指定轴数据去重</span><br>DataFrame.dropna() <span class="hljs-comment"># 删除缺失值行或列</span><br>DataFrame.pop(列标签) <span class="hljs-comment"># 删除列</span><br></code></pre></td></tr></table></figure></blockquote><h2 id="运算和统计"><a href="#运算和统计" class="headerlink" title="运算和统计"></a>运算和统计</h2><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs python">DataFrame.add(other) <span class="hljs-comment"># 加法运算</span><br>DataFrame.sub(other) <span class="hljs-comment"># 减法运算</span><br><br>DataFrame[列名] &gt; value <span class="hljs-comment"># 返回逻辑结果，布尔值</span><br>DataFrame[DataFrame[列名] &gt; value].head() <span class="hljs-comment"># 逻辑判断的结果可以作为筛选的依据</span><br>DataFrame.query(<span class="hljs-string">&quot;列名 &lt; value1 &amp; 列名 &gt; value2&quot;</span>).head() <span class="hljs-comment"># 进行多次逻辑判断</span><br>DataFrame[列名].isin([]) <span class="hljs-comment"># 可以指定值进行一个判断，从而进行筛选操作</span><br><br>DataFrame.describe() <span class="hljs-comment"># 综合分析: 能够直接得出很多统计结果,count, mean, std, min, max 等</span><br>DataFrame.<span class="hljs-built_in">sum</span>() <span class="hljs-comment"># 计算总和</span><br>DataFrame.mean() <span class="hljs-comment"># 计算平均值</span><br>DataFrame.median() <span class="hljs-comment"># 计算中位数</span><br>DataFrame.<span class="hljs-built_in">min</span>() <span class="hljs-comment"># 计算最小值</span><br>DataFrame.<span class="hljs-built_in">max</span>() <span class="hljs-comment"># 计算最大值</span><br>DataFrame.mode() <span class="hljs-comment"># 计算众数</span><br>DataFrame.<span class="hljs-built_in">abs</span>() <span class="hljs-comment"># 计算绝对值</span><br>DataFrame.prod() <span class="hljs-comment"># 计算连乘</span><br>DataFrame.std() <span class="hljs-comment"># 计算标准差</span><br>DataFrame.var() <span class="hljs-comment"># 计算方差</span><br>DataFrame.idxmax() <span class="hljs-comment"># 计算最大值索引</span><br>DataFrame.idxmin() <span class="hljs-comment"># 计算最小值索引</span><br>DataFrame.cumsum() <span class="hljs-comment"># 计算前1/2/3/…/n个数的和</span><br>DataFrame.cumprod() <span class="hljs-comment"># 计算前1/2/3/…/n个数的积</span><br></code></pre></td></tr></table></figure></blockquote><h2 id="高级操作"><a href="#高级操作" class="headerlink" title="高级操作"></a>高级操作</h2><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># Pandas画图</span><br>DataFrame.plot(kind=<span class="hljs-string">&#x27;line&#x27;</span>) <span class="hljs-comment"># kind表示需要绘制的种类，默认line,还有bar,barh,hist,pie,scatter</span><br><span class="hljs-comment"># 文件读取与存储</span><br>pandas.read_csv(path, usecols=[], engine=<span class="hljs-string">&#x27;python&#x27;</span>, encoding=<span class="hljs-string">&#x27;utf8&#x27;</span>) <span class="hljs-comment"># 读取csv文件</span><br>DataFrame.to_csv(path, columns=<span class="hljs-literal">None</span>, mode=<span class="hljs-string">&#x27;w&#x27;</span>, encoding=<span class="hljs-literal">None</span>) <span class="hljs-comment"># 转换成csv文件</span><br>pandas.read_hdf(path，key=<span class="hljs-literal">None</span>，** kwargs) <span class="hljs-comment"># 根据key读取hdf文件</span><br>DataFrame.to_hdf(path, key=, **kwargs*) <span class="hljs-comment"># 转换成hdf文件</span><br>pandas.read_json(path, lines=<span class="hljs-literal">False</span>) <span class="hljs-comment"># 读取json文件</span><br>DataFrame.to_json(path,lines=<span class="hljs-literal">False</span>) <span class="hljs-comment"># 转换成json文件</span><br><span class="hljs-comment"># 缺失值检测与填充</span><br>DataFrame.isna()|isnull()|notna()|notnull() <span class="hljs-comment"># 检测缺失值</span><br>DataFrame.replace(to_replace=, value=) <span class="hljs-comment"># to_replace替换前的值，value为替换后的值</span><br>DataFrame.fillna(<span class="hljs-number">0</span>) <span class="hljs-comment"># 用0替换缺失值</span><br>DataFrame.fillna(method=<span class="hljs-string">&#x27;pad&#x27;</span>) <span class="hljs-comment"># 使用缺失值前面的值进行填充</span><br>DataFrame.fillna(method=<span class="hljs-string">&#x27;bfill&#x27;</span>) <span class="hljs-comment"># 使用缺失值前面的值进行填充</span><br>DataFrame.interpolate(method=) <span class="hljs-comment"># 插值算法，平滑绘图&#x27;akima&#x27;，累计分布&#x27;pchip&#x27;，数据增长速率越来越快&#x27;quadratic&#x27;</span><br><span class="hljs-comment"># 数据离散化与数据合并</span><br>pandas.qcut(data, num) <span class="hljs-comment"># 将数据分为数量差不多的num个组，一般会与value_counts搭配使用，统计每组的个数</span><br>Series.value_counts() <span class="hljs-comment"># 统计分组次数</span><br>pandas.cut(data, bins) <span class="hljs-comment"># 自定义分组，bin为自定义列表</span><br>pandas.get_dummies(data, prefix=分组名字) <span class="hljs-comment"># 分组数据变成one-hot编码</span><br>pandas.concat([data1, data2], axis=<span class="hljs-number">1</span>) <span class="hljs-comment"># 按照行或列进行合并，0为行</span><br>pandas.merge(left, right, how=<span class="hljs-string">&#x27;inner&#x27;</span>, on=[]) <span class="hljs-comment"># 按照指定键指定方式进行连接</span><br><span class="hljs-comment"># 交叉表与透视表</span><br>pandas.crosstab(index,columns) <span class="hljs-comment"># 按index进行分组并作为行索引，并统计columns值的各个频数，交叉表</span><br>DataFrame.pivot_table(values=[], index=[],aggfunc=np.mean) <span class="hljs-comment"># 将values按照index分组，默认使用mean()聚合函数</span><br><span class="hljs-comment"># 分组与聚合</span><br>DataFrame.groupby(key, as_index=<span class="hljs-literal">False</span>) <span class="hljs-comment"># 按照keys进行分组</span><br>DataFrame.agg(&#123;<span class="hljs-string">&quot;列名&quot;</span>：np.聚合函数&#125;) <span class="hljs-comment"># 聚合函数为max(),min(),mean(),std()等函数</span><br></code></pre></td></tr></table></figure></blockquote><h1 id="Matplotlib"><a href="#Matplotlib" class="headerlink" title="Matplotlib"></a>Matplotlib</h1><blockquote><table><thead><tr><th>方法</th><th>含义</th></tr></thead><tbody><tr><td><code>matplotlib.pyplot.plot</code></td><td>绘制折线图</td></tr><tr><td><code>matplotlib.pyplot.bar</code></td><td>绘制柱状图</td></tr><tr><td><code>matplotlib.pyplot.barh</code></td><td>绘制直方图</td></tr><tr><td><code>matplotlib.pyplot.broken_barh</code></td><td>绘制水平直方图</td></tr><tr><td><code>matplotlib.pyplot.contour</code></td><td>绘制等高线图</td></tr><tr><td><code>matplotlib.pyplot.errorbar</code></td><td>绘制误差线</td></tr><tr><td><code>matplotlib.pyplot.hexbin</code></td><td>绘制六边形图案</td></tr><tr><td><code>matplotlib.pyplot.hist</code></td><td>绘制柱形图</td></tr><tr><td><code>matplotlib.pyplot.hist2d</code></td><td>绘制水平柱状图</td></tr><tr><td><code>matplotlib.pyplot.pie</code></td><td>绘制饼状图</td></tr><tr><td><code>matplotlib.pyplot.quiver</code></td><td>绘制量场图</td></tr><tr><td><code>matplotlib.pyplot.scatter</code></td><td>散点图</td></tr><tr><td><code>matplotlib.pyplot.specgram</code></td><td>绘制光谱图</td></tr></tbody></table></blockquote><h2 id="简单绘制流程"><a href="#简单绘制流程" class="headerlink" title="简单绘制流程"></a>简单绘制流程</h2><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 基础折线图</span><br>plt.figure(figsize=(),dpi=) <span class="hljs-comment"># 新建图形对象,通过 figsize 调节尺寸, dpi 调节显示精度</span><br>plt.plot(x, y, label=) <span class="hljs-comment"># 绘制图像和设置图例名称</span><br>plt.xticks(x,显示格式) <span class="hljs-comment"># 设置x轴刻度</span><br>plt.yticks(y,显示格式) <span class="hljs-comment"># 设置x轴刻度</span><br>plt.xlim() <span class="hljs-comment"># 设置x坐标轴范围</span><br>plt.ylim() <span class="hljs-comment"># 设置y坐标轴范围</span><br>plt.xlabel() <span class="hljs-comment"># 设置x轴标题</span><br>plt.ylabel() <span class="hljs-comment"># 设置y轴标题</span><br>plt.title() <span class="hljs-comment"># 设置标题</span><br>plt.grid(<span class="hljs-literal">True</span>, linestyle=, alpha=) <span class="hljs-comment"># 设置网格线，及线类型，透明度</span><br>plt.text(x,y,s) <span class="hljs-comment"># 图形标注，x, y 用于标注位置定位，s 代表标注的字符串</span><br>plt.savefig(<span class="hljs-string">&quot;&quot;</span>) <span class="hljs-comment"># 图像保存</span><br>plt.legend(loc=) <span class="hljs-comment"># 以对应的位置上添加图例</span><br>plt.show() <span class="hljs-comment"># 图像显示</span><br><span class="hljs-comment"># 大图套小图</span><br>fig = plt.figure(figsize=(),dpi=)<br>axes = fig.add_axes([])  <span class="hljs-comment"># 控制画布的左, 下, 宽度, 高度来添加画布</span><br>axes.plot(x, y)<br><span class="hljs-comment"># 多行多列图</span><br>fig, axes = plt.subplots(nrows=, ncols=)  <span class="hljs-comment"># 创建多行多列的图像对象及画布</span><br>axes.set_xticks(x,显示格式) <span class="hljs-comment"># 设置x轴刻度</span><br>axes.set_yticks(y,显示格式) <span class="hljs-comment"># 设置x轴刻度</span><br>axes.set_xlabel() <span class="hljs-comment"># 设置x轴标题</span><br>axes.set_ylabel() <span class="hljs-comment"># 设置y轴标题</span><br>axes.set_title() <span class="hljs-comment"># 设置标题</span><br></code></pre></td></tr></table></figure><h2 id="图形样式"><a href="#图形样式" class="headerlink" title="图形样式"></a>图形样式</h2><blockquote><table><thead><tr><th>参数</th><th>含义</th></tr></thead><tbody><tr><td><code>alpha=</code></td><td>设置线型的透明度，从 0.0 到 1.0</td></tr><tr><td><code>color=</code></td><td>设置线型的颜色</td></tr><tr><td><code>fillstyle=</code></td><td>设置线型的填充样式</td></tr><tr><td><code>linestyle=</code></td><td>设置线型的样式</td></tr><tr><td><code>linewidth=</code></td><td>设置线型的宽度</td></tr><tr><td><code>marker=</code></td><td>设置标记点的样式</td></tr></tbody></table></blockquote><h1 id="Seaborn"><a href="#Seaborn" class="headerlink" title="Seaborn"></a>Seaborn</h1><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><code class="hljs python">sns.<span class="hljs-built_in">set</span>(context=<span class="hljs-string">&#x27;notebook&#x27;</span>, style=<span class="hljs-string">&#x27;darkgrid&#x27;</span>, palette=<span class="hljs-string">&#x27;deep&#x27;</span>) <span class="hljs-comment"># 声名样式</span><br><span class="hljs-comment"># 分布图</span><br>seaborn.distplot(a, bins=<span class="hljs-literal">None</span>, hist=<span class="hljs-literal">True</span>, kde=<span class="hljs-literal">True</span>, rug=<span class="hljs-literal">False</span>, fit=<span class="hljs-literal">None</span>, color=<span class="hljs-literal">None</span>) <span class="hljs-comment"># 单变量直方图核密度</span><br>seaborn.jointplot(x, y, data=<span class="hljs-literal">None</span>, kind=<span class="hljs-string">&#x27;scatter&#x27;</span>) <span class="hljs-comment"># 多变量相互关系散点图</span><br>sns.pairplot(data) <span class="hljs-comment"># 绘制多个成对的双变量分布</span><br><span class="hljs-comment"># 类别图</span><br>seaborn.catplot(x=<span class="hljs-literal">None</span>, y=<span class="hljs-literal">None</span>, kind=<span class="hljs-string">&quot;strip&quot;</span>, data=<span class="hljs-literal">None</span>) <span class="hljs-comment"># Figure-level 接口</span><br>seaborn.stripplot(x=<span class="hljs-literal">None</span>, y=<span class="hljs-literal">None</span>, hue=<span class="hljs-literal">None</span>, data=<span class="hljs-literal">None</span>, jitter=<span class="hljs-literal">False</span>) <span class="hljs-comment"># 分类散点线图，数据部分重合</span><br>seaborn.swarmplot(x=, y=, data=) <span class="hljs-comment"># 分类散点线图，数据不重合</span><br>seaborn.boxplot(x=<span class="hljs-literal">None</span>, y=<span class="hljs-literal">None</span>, hue=<span class="hljs-literal">None</span>, data=<span class="hljs-literal">None</span>) <span class="hljs-comment"># 箱体图，示出一组数据的最大值、最小值、中位数、及上下四分位数</span><br>seaborn.violinplot(x=<span class="hljs-literal">None</span>, y=<span class="hljs-literal">None</span>, hue=<span class="hljs-literal">None</span>, data=<span class="hljs-literal">None</span>) <span class="hljs-comment"># 提琴图，显示数据分布及其概率密度</span><br>seaborn.barplot(x=<span class="hljs-literal">None</span>, y=<span class="hljs-literal">None</span>, hue=<span class="hljs-literal">None</span>, data=<span class="hljs-literal">None</span>) <span class="hljs-comment"># 条形图，显示95%置信区间</span><br>seaborn.pointplot(x=<span class="hljs-literal">None</span>, y=<span class="hljs-literal">None</span>, hue=<span class="hljs-literal">None</span>, data=<span class="hljs-literal">None</span>) <span class="hljs-comment"># 点图，只绘制点图和置信区间</span><br><span class="hljs-comment"># 关联图</span><br>seaborn.relplot(x=, y=,hue=, style=, kind=<span class="hljs-string">&quot;scatter&quot;</span>|<span class="hljs-string">&quot;line&quot;</span>, data=) <span class="hljs-comment"># Figure-level 接口</span><br><span class="hljs-comment"># 回归图</span><br>seaborn.regplot(x=<span class="hljs-literal">None</span>, y=<span class="hljs-literal">None</span>, data=<span class="hljs-literal">None</span>) <span class="hljs-comment"># 自动完成线性回归拟合</span><br>seaborn.lmplot(x=<span class="hljs-literal">None</span>, y=<span class="hljs-literal">None</span>, hue=<span class="hljs-literal">None</span>, data=<span class="hljs-literal">None</span>) <span class="hljs-comment"># 引入第三维度进行对比，自动完成线性回归拟合</span><br><span class="hljs-comment"># 矩阵图</span><br>seaborn.heatmap(a,square,linewidths,annot) <span class="hljs-comment"># 绘制热力图</span><br>seaborn.clustermap(DataFrame) <span class="hljs-comment"># 支持绘制层次聚类结构图</span><br></code></pre></td></tr></table></figure></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>Seaborn</title>
      <link href="2021/01/29/Seaborn/"/>
      <url>2021/01/29/Seaborn/</url>
      
        <content type="html"><![CDATA[<blockquote><p>Seaborn基于 Matplotlib核心库进行了更高级的API封装，可以轻松地画出更漂亮的图形，而Seaborn的漂亮主要体现在配色更加舒服，以及图形元素的样式更加细腻。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 安装 </span><br>pip install seaborn==<span class="hljs-number">0.10</span><span class="hljs-number">.1</span><br><span class="hljs-comment"># 导入</span><br><span class="hljs-keyword">import</span> seaborn <span class="hljs-keyword">as</span> sns<br></code></pre></td></tr></table></figure><p>使用 Seaborn 完成图像快速优化的方法非常简单。只需要将 Seaborn 提供的样式声明代码 <code>sns.set()</code> 放置在绘图前即可。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">sns.<span class="hljs-built_in">set</span>(context=<span class="hljs-string">&#x27;notebook&#x27;</span>, style=<span class="hljs-string">&#x27;darkgrid&#x27;</span>, palette=<span class="hljs-string">&#x27;deep&#x27;</span>, font=<span class="hljs-string">&#x27;sans-serif&#x27;</span>, font_scale=<span class="hljs-number">1</span>, color_codes=<span class="hljs-literal">False</span>, rc=<span class="hljs-literal">None</span>)<br></code></pre></td></tr></table></figure><ul><li><code>context=&#39;&#39;</code> 参数控制着默认的画幅大小，分别有 <code>&#123;paper, notebook, talk, poster&#125;</code> 四个值。其中，<code>poster &gt; talk &gt; notebook &gt; paper</code>。</li><li><code>style=&#39;&#39;</code> 参数控制默认样式，分别有 <code>&#123;darkgrid, whitegrid, dark, white, ticks&#125;</code>，你可以自行更改查看它们之间的不同。</li><li><code>palette=&#39;&#39;</code> 参数为预设的调色板。分别有 <code>&#123;deep, muted, bright, pastel, dark, colorblind&#125;</code> 等，你可以自行更改查看它们之间的不同。</li><li>剩下的 <code>font=&#39;&#39;</code> 用于设置字体，<code>font_scale=</code> 设置字体大小，<code>color_codes=</code> 不使用调色板而采用先前的 <code>&#39;r&#39;</code> 等色彩缩写。</li></ul></blockquote><h1 id="分布图"><a href="#分布图" class="headerlink" title="分布图"></a>分布图</h1><h2 id="绘制单变量分布"><a href="#绘制单变量分布" class="headerlink" title="绘制单变量分布"></a>绘制单变量分布</h2><blockquote><p>可以采用最简单的直方图描述单变量的分布情况。 Seaborn中提供了<code> distplot()</code>函数，它默认绘制的是一个带有核密度估计曲线的直方图。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs Python">seaborn.distplot(a, bins=<span class="hljs-literal">None</span>, hist=<span class="hljs-literal">True</span>, kde=<span class="hljs-literal">True</span>, rug=<span class="hljs-literal">False</span>, fit=<span class="hljs-literal">None</span>, color=<span class="hljs-literal">None</span>)<br></code></pre></td></tr></table></figure><p>上述函数中常用参数的含义如下：</p><ul><li><code>a</code>：表示要观察的数据，可以是 Series、一维数组或列表。</li><li><code>bins</code>：用于控制条形的数量。</li><li><code>hist</code>：接收布尔类型，表示<code>是否绘制(标注)直方图</code>。</li><li><code>kde</code>：接收布尔类型，表示<code>是否绘制高斯核密度</code>估计曲线。</li><li><code>rug</code>：接收布尔类型，表示是否在支持的轴方向上绘制rugplot。</li></ul><p>例：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">import</span> numpy <span class="hljs-keyword">as</span> np<br>sns.<span class="hljs-built_in">set</span>() <span class="hljs-comment"># 调用set()函数获取默认绘图</span><br>np.random.seed(<span class="hljs-number">0</span>)  <span class="hljs-comment"># 确定随机数生成器的种子,如果不使用每次生成图形不一样</span><br>arr = np.random.randn(<span class="hljs-number">100</span>)  <span class="hljs-comment"># 生成随机数组</span><br>ax = sns.distplot(arr, bins=<span class="hljs-number">10</span>, hist=<span class="hljs-literal">True</span>, kde=<span class="hljs-literal">True</span>, rug=<span class="hljs-literal">True</span>)  <span class="hljs-comment"># 绘制直方图</span><br></code></pre></td></tr></table></figure></blockquote><h2 id="绘制双变量分布"><a href="#绘制双变量分布" class="headerlink" title="绘制双变量分布"></a>绘制双变量分布</h2><blockquote><p>两个变量的二元分布可视化也很有用。在 Seaborn中最简单的方法是使用 <code>jointplot()</code>函数，该函数可以创建一个多面板图形，比如散点图、二维直方图、核密度估计等，以显示两个变量之间的双变量关系及每个变量在单坐标轴上的单变量分布。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python">seaborn.jointplot(x, y, data=<span class="hljs-literal">None</span>, <br>                  kind=<span class="hljs-string">&#x27;scatter&#x27;</span>, stat_func=<span class="hljs-literal">None</span>, color=<span class="hljs-literal">None</span>, <br>                  ratio=<span class="hljs-number">5</span>, space=<span class="hljs-number">0.2</span>, dropna=<span class="hljs-literal">True</span>)<br></code></pre></td></tr></table></figure><p>上述函数中常用参数的含义如下：</p><ol><li><code>kind</code>：表示绘制图形的类型。hex:直方图，kde：核函数概率，reg：拟合线</li><li><code>stat_func</code>：用于计算有关关系的统计量并标注图。</li><li><code>color</code>：表示绘图元素的颜色。</li><li><code>size</code>：用于设置图的大小(正方形)。</li><li><code>ratio</code>：表示中心图与侧边图的比例。该参数的值越大，则中心图的占比会越大。</li><li><code>space</code>：用于设置中心图与侧边图的间隔大小。</li></ol></blockquote><h2 id="绘制成对的双变量分布"><a href="#绘制成对的双变量分布" class="headerlink" title="绘制成对的双变量分布"></a>绘制成对的双变量分布</h2><blockquote><p>要想在数据集中绘制多个成对的双变量分布，则可以使用<code>pairplot()</code>函数实现，该函数会创建一个坐标轴矩阵，并且显示Datafram对象中每对变量的关系。另外，<code>pairplot()</code>函数也可以绘制每个变量在对角轴上的单变量分布。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 绘制多个成对的双变量分布</span><br>sns.pairplot(data)<br></code></pre></td></tr></table></figure></blockquote><h1 id="类别图"><a href="#类别图" class="headerlink" title="类别图"></a>类别图</h1><blockquote><p>类别图的 Figure-level 接口是 <code>catplot</code>，其为 categorical plots 的缩写。而 <code>catplot</code> 实际上是如下 Axes-level 绘图 API 的集合。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">sns.catplot(x=<span class="hljs-literal">None</span>, y=<span class="hljs-literal">None</span>, kind=<span class="hljs-string">&quot;strip&quot;</span>, data=<span class="hljs-literal">None</span>)<br></code></pre></td></tr></table></figure><p><code>参数kind：</code></p><ol><li>分类散点图：<code>strip，swarm</code></li><li>分类分布图：<code>box，violin，boxen</code></li><li>分类估计图：<code>point，bar，count</code></li></ol><p>Seaborn针对分类数据提供了专门的可视化函数，这些函数大致可以分为如下三种:</p><ul><li>分类数据散点图: <code>swarmplot()</code>与 <code>stripplot()</code>。</li><li>类数据的分布图: <code>boxplot()</code> 与 <code>violinplot()</code>。</li><li>分类数据的统计估算图:<code>barplot()</code> 与<code> pointplot()</code>。</li></ul></blockquote><h2 id="散点图"><a href="#散点图" class="headerlink" title="散点图"></a>散点图</h2><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">seaborn.stripplot(x=<span class="hljs-literal">None</span>, y=<span class="hljs-literal">None</span>, hue=<span class="hljs-literal">None</span>, data=<span class="hljs-literal">None</span>, order=<span class="hljs-literal">None</span>, hue_order=<span class="hljs-literal">None</span>, jitter=<span class="hljs-literal">False</span>)<br></code></pre></td></tr></table></figure><p>上述函数中常用参数的含义如下</p><ul><li><code>x，y，hue</code>：用于绘制长格式数据的输入。</li><li><code>data</code>：用于绘制的数据集。如果x和y不存在，则它将作为宽格式，否则将作为长格式。</li><li> <code>jitter</code>：表示抖动的程度(仅沿类別轴)。当很多数据点重叠时，可以指定抖动的数量或者设为True使用默认值。</li></ul><p>还可调用<code> swarmplot()</code>函数绘制散点图，该函数的好处是所有的数据点都不会重叠，可以很清晰地观察到数据的分布情况</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">sns.swarmplot(x=<span class="hljs-string">&quot;day&quot;</span>, y=<span class="hljs-string">&quot;total_bill&quot;</span>, data=tips)<br></code></pre></td></tr></table></figure></blockquote><h2 id="类别内的数据分布"><a href="#类别内的数据分布" class="headerlink" title="类别内的数据分布"></a>类别内的数据分布</h2><blockquote><p><strong>箱形图</strong>:</p><ul><li>箱形图（Box-plot）又称为盒须图、盒式图或箱线图，是一种用作显示一组数据分散情况资料的统计图。</li><li><strong>它能显示出一组数据的最大值、最小值、中位数、及上下四分位数。</strong></li></ul><p><strong>小提琴图:</strong></p><ul><li>小提琴图 (Violin Plot) 用于显示数据分布及其概率密度。</li><li><strong>这种图表结合了箱形图和密度图的特征，主要用来显示数据的分布形状。</strong></li><li><strong>中间的黑色粗条表示四分位数范围，从其延伸的幼细黑线代表 95% 置信区间（样本可能来自此区间的概率为95%），而白点则为中位数。</strong></li><li><strong>箱形图在数据显示方面受到限制，简单的设计往往隐藏了有关数据分布的重要细节。</strong>例如使用箱形图时，我们不能了解数据分布。虽然小提琴图可以显示更多详情，但它们也可能包含较多干扰信息。</li></ul></blockquote><h3 id="绘制箱体图"><a href="#绘制箱体图" class="headerlink" title="绘制箱体图"></a>绘制箱体图</h3><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">seaborn.boxplot(x=<span class="hljs-literal">None</span>, y=<span class="hljs-literal">None</span>, hue=<span class="hljs-literal">None</span>, data=<span class="hljs-literal">None</span>, orient=<span class="hljs-literal">None</span>, color=<span class="hljs-literal">None</span>,  saturation=<span class="hljs-number">0.75</span>, width=<span class="hljs-number">0.8</span>)<br></code></pre></td></tr></table></figure><ol><li><code>palette</code>：用于设置不同级别色相的颜色变量。—- palette=[“r”,”g”,”b”,”y”]</li><li><code>saturation</code>：用于设置数据显示的颜色饱和度。—- 使用小数表示</li></ol></blockquote><h3 id="绘制提琴图"><a href="#绘制提琴图" class="headerlink" title="绘制提琴图"></a>绘制提琴图</h3><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">seaborn.violinplot(x=<span class="hljs-literal">None</span>, y=<span class="hljs-literal">None</span>, hue=<span class="hljs-literal">None</span>, data=<span class="hljs-literal">None</span>)<br></code></pre></td></tr></table></figure></blockquote><h2 id="类别内的统计估计"><a href="#类别内的统计估计" class="headerlink" title="类别内的统计估计"></a>类别内的统计估计</h2><blockquote><p>要想查看每个分类的集中趋势，则可以使用条形图和点图进行展示。 Seaborn库中用于绘制这两种图表的具体函数如下</p><ul><li><code>barplot()</code>函数：绘制条形图。</li><li><code>pointplot()</code>函数：绘制点图。</li></ul></blockquote><h3 id="绘制条形图"><a href="#绘制条形图" class="headerlink" title="绘制条形图"></a>绘制条形图</h3><blockquote><p>最常用的查看集中趋势的图形就是条形图。默认情况下， barplot函数会在整个数据集上使用均值进行估计。**若每个类别中有多个类别时(使用了hue参数)<strong>，则条形图可以使用引导来计算估计的</strong>置信区间(是指由样本统计量所构造的总体参数的估计区间)**，并使用误差条（图中黑色的竖线）来表示出现这个均值的95%置信区间。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">sns.barplot(x=<span class="hljs-string">&quot;day&quot;</span>, y=<span class="hljs-string">&quot;total_bill&quot;</span>, data=tips)<br></code></pre></td></tr></table></figure></blockquote><h3 id="绘制点图"><a href="#绘制点图" class="headerlink" title="绘制点图"></a>绘制点图</h3><blockquote><p>另外一种用于估计的图形是点图，可以调用 pointplot()函数进行绘制，该函数会用高度低计值对数据进行描述，而不是显示完整的条形，<strong>它只会绘制点估计和置信区间</strong>。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">sns.pointplot(x=<span class="hljs-string">&quot;day&quot;</span>, y=<span class="hljs-string">&quot;total_bill&quot;</span>, data=tips)<br></code></pre></td></tr></table></figure></blockquote><h1 id="关联图"><a href="#关联图" class="headerlink" title="关联图"></a>关联图</h1><blockquote><p>当我们需要对数据进行关联性分析时，可能会用到 Seaborn 提供的以下几个 API。</p><table><thead><tr><th align="center">关联性分析</th><th align="center">介绍</th></tr></thead><tbody><tr><td align="center">relplot</td><td align="center">绘制关系图</td></tr><tr><td align="center">scatterplot</td><td align="center">多维度分析散点图</td></tr><tr><td align="center">lineplot</td><td align="center">多维度分析线形图</td></tr></tbody></table><p><code>relplot</code>是 relational plots 的缩写，其可以用于呈现数据之后的关系，主要有散点图和条形图 2 种样式。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">sns.relplot(x=, y=,hue=, style=, kind=<span class="hljs-string">&quot;scatter&quot;</span>, data=)<br></code></pre></td></tr></table></figure><p>参数：</p><ol><li><code>hue：</code>加入类别特征对数据进行着色</li><li><code>style：</code> 可以赋予不同类别的散点不同的形状</li><li><code>kind:</code>还支持绘制线图，只需要<code>kind=&quot;line&quot;</code>线形态绘制时还会自动给出 95% 的置信区间。</li></ol><p><strong>API层级概念：</strong></p><p>Seaborn 中的 API 分为 Figure-level 和 Axes-level 两种。<code>relplot</code> 就是一个 Figure-level 接口，而 <code>scatterplot</code> 和 <code>lineplot</code> 则是 Axes-level 接口。</p></blockquote><h1 id="回归图"><a href="#回归图" class="headerlink" title="回归图"></a>回归图</h1><blockquote><p>回归图的绘制函数主要有：<code>lmplot</code>和<code>regplot</code></p><p><code>regplot</code> 绘制回归图时，只需要指定自变量和因变量即可，<code>regplot</code> 会自动完成线性回归拟合。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">sns.regplot(x=<span class="hljs-string">&quot;sepal_length&quot;</span>, y=<span class="hljs-string">&quot;sepal_width&quot;</span>, data=iris)<br></code></pre></td></tr></table></figure><p><code>lmplot</code> 同样是用于绘制回归图，但 <code>lmplot</code> 支持引入第三维度进行对比，例如我们设置 <code>hue=&quot;species&quot;</code>。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">sns.lmplot(x=<span class="hljs-string">&quot;sepal_length&quot;</span>, y=<span class="hljs-string">&quot;sepal_width&quot;</span>, hue=<span class="hljs-string">&quot;species&quot;</span>, data=iris)<br></code></pre></td></tr></table></figure></blockquote><h1 id="矩阵图"><a href="#矩阵图" class="headerlink" title="矩阵图"></a>矩阵图</h1><blockquote><p>矩阵图中最常用的就只有 2 个，分别是：<code>heatmap</code>和<code>clustermap</code></p><p><code>heatmap</code> 主要用于绘制热力图。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">sns.heatmap(a,square,linewidths,annot)<br></code></pre></td></tr></table></figure><p><code>clustermap</code> 支持绘制<code>层次聚类</code>结构图</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">iris.pop(<span class="hljs-string">&quot;species&quot;</span>)<br>sns.clustermap(iris)<br></code></pre></td></tr></table></figure></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>Matplotlib</title>
      <link href="2020/12/17/Matplotlib/"/>
      <url>2020/12/17/Matplotlib/</url>
      
        <content type="html"><![CDATA[<blockquote><p><strong>注意：</strong></p><p>在使用 Notebook 环境绘图时，需要先运行 Jupyter Notebook 的魔术命令 <code>%matplotlib inline</code>。这条命令的作用是将 Matplotlib 绘制的图形嵌入在当前页面中。而在桌面环境中绘图时，不需要添加此命令，而是在全部绘图代码之后追加 <code>plt.show()</code>。</p></blockquote><h1 id="简单图形绘制"><a href="#简单图形绘制" class="headerlink" title="简单图形绘制"></a>简单图形绘制</h1><blockquote><p>需要导入 <code>pyplot</code> 模块，并约定简称为 <code>plt</code></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">from</span> matplotlib <span class="hljs-keyword">import</span> pyplot <span class="hljs-keyword">as</span> plt<br>plt.plot([<span class="hljs-number">2</span>, <span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>, <span class="hljs-number">4</span>, <span class="hljs-number">5</span>, <span class="hljs-number">6</span>, <span class="hljs-number">5</span>, <span class="hljs-number">4</span>, <span class="hljs-number">3</span>, <span class="hljs-number">2</span>, <span class="hljs-number">1</span>])<br></code></pre></td></tr></table></figure><p><code>plt.plot()</code> 是 <code>pyplot</code> 模块下面的直线绘制（折线图）方法类。示例中包含了一个 <code>[2, 1, 2, 3, 4, 5, 6, 5, 4, 3, 2, 1]</code> 列表，Matplotlib 会默认将该列表作为 𝑦y 值，而 𝑥x 值会从 00 开始依次递增。</p><p>当然，如果你需要自定义横坐标值，只需要传入两个列表即可。</p></blockquote><table><thead><tr><th>方法</th><th>含义</th></tr></thead><tbody><tr><td><code>matplotlib.pyplot.angle_spectrum</code></td><td>绘制电子波谱图</td></tr><tr><td><code>matplotlib.pyplot.bar</code></td><td>绘制柱状图</td></tr><tr><td><code>matplotlib.pyplot.barh</code></td><td>绘制直方图</td></tr><tr><td><code>matplotlib.pyplot.broken_barh</code></td><td>绘制水平直方图</td></tr><tr><td><code>matplotlib.pyplot.contour</code></td><td>绘制等高线图</td></tr><tr><td><code>matplotlib.pyplot.errorbar</code></td><td>绘制误差线</td></tr><tr><td><code>matplotlib.pyplot.hexbin</code></td><td>绘制六边形图案</td></tr><tr><td><code>matplotlib.pyplot.hist</code></td><td>绘制柱形图</td></tr><tr><td><code>matplotlib.pyplot.hist2d</code></td><td>绘制水平柱状图</td></tr><tr><td><code>matplotlib.pyplot.pie</code></td><td>绘制饼状图</td></tr><tr><td><code>matplotlib.pyplot.quiver</code></td><td>绘制量场图</td></tr><tr><td><code>matplotlib.pyplot.scatter</code></td><td>散点图</td></tr><tr><td><code>matplotlib.pyplot.specgram</code></td><td>绘制光谱图</td></tr></tbody></table><h1 id="图形样式"><a href="#图形样式" class="headerlink" title="图形样式"></a>图形样式</h1><h2 id="定义图形样式"><a href="#定义图形样式" class="headerlink" title="定义图形样式"></a>定义图形样式</h2><blockquote><p>线形图通过 <code>matplotlib.pyplot.plot(*args, **kwargs)</code> 方法绘出。其中，<code>args</code> 代表数据输入，而 <code>kwargs</code> 的部分就是用于设置样式参数了。</p><table><thead><tr><th>参数</th><th>含义</th></tr></thead><tbody><tr><td><code>alpha=</code></td><td>设置线型的透明度，从 0.0 到 1.0</td></tr><tr><td><code>color=</code></td><td>设置线型的颜色</td></tr><tr><td><code>fillstyle=</code></td><td>设置线型的填充样式</td></tr><tr><td><code>linestyle=</code></td><td>设置线型的样式</td></tr><tr><td><code>linewidth=</code></td><td>设置线型的宽度</td></tr><tr><td><code>marker=</code></td><td>设置标记点的样式</td></tr><tr><td>……</td><td>……</td></tr></tbody></table></blockquote><blockquote><p>散点图也是相似的，它们的很多样式参数都是大同小异</p><table><thead><tr><th>参数</th><th>含义</th></tr></thead><tbody><tr><td><code>s=</code></td><td>散点大小</td></tr><tr><td><code>c=</code></td><td>散点颜色</td></tr><tr><td><code>marker=</code></td><td>散点样式</td></tr><tr><td><code>cmap=</code></td><td>定义多类别散点的颜色</td></tr><tr><td><code>alpha=</code></td><td>点的透明度</td></tr><tr><td><code>edgecolors=</code></td><td>散点边缘颜色</td></tr></tbody></table></blockquote><h2 id="定义图形位置"><a href="#定义图形位置" class="headerlink" title="定义图形位置"></a>定义图形位置</h2><blockquote><p>在图形的绘制过程中，你可能需要调整图形的位置，或者把几张单独的图形拼接在一起。此时，我们就需要引入 <code>plt.figure</code> 图形对象了。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">import</span> numpy <span class="hljs-keyword">as</span> np<br>x = np.linspace(<span class="hljs-number">0</span>, <span class="hljs-number">10</span>, <span class="hljs-number">20</span>)  <span class="hljs-comment"># 生成数据</span><br>y = x * x + <span class="hljs-number">2</span><br><br>fig = plt.figure(figsize=(元组),dpi=)  <span class="hljs-comment"># 新建图形对象,通过 figsize 调节尺寸, dpi 调节显示精度</span><br>axes = fig.add_axes([<span class="hljs-number">0.5</span>, <span class="hljs-number">0.5</span>, <span class="hljs-number">0.8</span>, <span class="hljs-number">0.8</span>])  <span class="hljs-comment"># 控制画布的左, 下, 宽度, 高度</span><br>axes.plot(x, y, <span class="hljs-string">&#x27;r&#x27;</span>)<br></code></pre></td></tr></table></figure><p><strong>大图套小图</strong></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs python">fig = plt.figure()  <span class="hljs-comment"># 新建画板</span><br>axes1 = fig.add_axes([<span class="hljs-number">0.1</span>, <span class="hljs-number">0.1</span>, <span class="hljs-number">0.8</span>, <span class="hljs-number">0.8</span>])  <span class="hljs-comment"># 大画布</span><br>axes2 = fig.add_axes([<span class="hljs-number">0.2</span>, <span class="hljs-number">0.5</span>, <span class="hljs-number">0.4</span>, <span class="hljs-number">0.3</span>])  <span class="hljs-comment"># 小画布</span><br><br>axes1.plot(x, y, <span class="hljs-string">&#x27;r&#x27;</span>)  <span class="hljs-comment"># 大画布</span><br>axes2.plot(y, x, <span class="hljs-string">&#x27;g&#x27;</span>)  <span class="hljs-comment"># 小画布</span><br></code></pre></td></tr></table></figure><p>Matplotlib 中，还有一种添加画布的方式，那就是 <code>plt.subplots()</code>，它和 <code>axes</code> 都等同于画布。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs plain">fig, axes &#x3D; plt.subplots()<br>axes.plot(x, y, &#39;r&#39;)<br></code></pre></td></tr></table></figure><p>借助于 <code>plt.subplots()</code>，我们就可以实现子图的绘制，也就是将多张图按一定顺序拼接在一起。</p><p>其中axes设置标题方法不同：</p><p>​    set_xticks</p><p>​    set_yticks</p><p>​    set_xlabel</p><p>​    set_ylabel</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs plain">fig, axes &#x3D; plt.subplots(nrows&#x3D;1, ncols&#x3D;2)  # 子图为 1 行，2 列<br>for ax in axes:<br> ax.plot(x, y, &#39;r&#39;)<br></code></pre></td></tr></table></figure><p>通过设置 <code>plt.subplots</code> 的参数，可以实现调节画布尺寸和显示精度</p><p>。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs plain">fig, axes &#x3D; plt.subplots(figsize&#x3D;(16, 9), dpi&#x3D;50)  # 通过 figsize 调节尺寸, dpi 调节显示精度<br>axes.plot(x, y, &#39;r&#39;)<br></code></pre></td></tr></table></figure></blockquote><h2 id="规范绘图方法"><a href="#规范绘图方法" class="headerlink" title="规范绘图方法"></a>规范绘图方法</h2><h3 id="添加图标题、图例"><a href="#添加图标题、图例" class="headerlink" title="添加图标题、图例"></a>添加图标题、图例</h3><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs python">fig, axes = plt.subplots()<br><br>axes.set_xlabel(<span class="hljs-string">&#x27;x label&#x27;</span>)  <span class="hljs-comment"># 横轴名称</span><br>axes.set_ylabel(<span class="hljs-string">&#x27;y label&#x27;</span>)<br>axes.set_title(<span class="hljs-string">&#x27;title&#x27;</span>)  <span class="hljs-comment"># 图形名称</span><br><br>axes.plot(x, x**<span class="hljs-number">2</span>) <span class="hljs-comment"># 添加图例也可以传入label参数</span><br>axes.plot(x, x**<span class="hljs-number">3</span>)<br>axes.legend([<span class="hljs-string">&quot;y = x**2&quot;</span>, <span class="hljs-string">&quot;y = x**3&quot;</span>], loc=<span class="hljs-number">0</span>) <br></code></pre></td></tr></table></figure><p><code>loc</code> 参数标记图例位置，<code>1，2，3，4</code> 依次代表：右上角、左上角、左下角，右下角；<code>0</code> 代表自适应。</p></blockquote><h3 id="添加自定义刻度"><a href="#添加自定义刻度" class="headerlink" title="添加自定义刻度"></a>添加自定义刻度</h3><figure class="highlight matlab"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs matlab"># 构造x轴刻度标签<br>x_ticks_label = [<span class="hljs-string">&quot;11点&#123;&#125;分&quot;</span>.format(<span class="hljs-built_in">i</span>) <span class="hljs-keyword">for</span> <span class="hljs-built_in">i</span> in x]<br># 构造y轴刻度<br>y_ticks = range(<span class="hljs-number">40</span>)<br># 修改x,y轴坐标的刻度显示<br>plt.xticks(x[::<span class="hljs-number">5</span>], x_ticks_label[::<span class="hljs-number">5</span>])<br>plt.yticks(y_ticks[::<span class="hljs-number">5</span>])<br></code></pre></td></tr></table></figure><blockquote><p>显示中文字体</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">from</span> pylab <span class="hljs-keyword">import</span> mpl<br><span class="hljs-comment"># 设置显示中文字体</span><br>mpl.rcParams[<span class="hljs-string">&quot;font.sans-serif&quot;</span>] = [<span class="hljs-string">&quot;SimHei&quot;</span>]<br><span class="hljs-comment"># 坐标轴无法正常显示中文时</span><br>mpl.rcParams[<span class="hljs-string">&quot;axes.unicode_minus&quot;</span>] = <span class="hljs-literal">False</span><br></code></pre></td></tr></table></figure></blockquote><h3 id="线型、颜色、透明度"><a href="#线型、颜色、透明度" class="headerlink" title="线型、颜色、透明度"></a>线型、颜色、透明度</h3><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python">fig, axes = plt.subplots()<br>axes.plot(x, x+<span class="hljs-number">1</span>, color=<span class="hljs-string">&quot;red&quot;</span>, alpha=<span class="hljs-number">0.5</span>)<span class="hljs-comment"># 颜色和透明度</span><br>axes.plot(x, x+<span class="hljs-number">2</span>, color=<span class="hljs-string">&quot;#1155dd&quot;</span>)<br></code></pre></td></tr></table></figure><p>更多线型</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><code class="hljs python">fig, ax = plt.subplots(figsize=(<span class="hljs-number">12</span>, <span class="hljs-number">6</span>))<br><br><span class="hljs-comment"># 线宽</span><br>ax.plot(x, x+<span class="hljs-number">1</span>, color=<span class="hljs-string">&quot;blue&quot;</span>, linewidth=<span class="hljs-number">0.25</span>)<br>ax.plot(x, x+<span class="hljs-number">2</span>, color=<span class="hljs-string">&quot;blue&quot;</span>, linewidth=<span class="hljs-number">0.50</span>)<br>ax.plot(x, x+<span class="hljs-number">3</span>, color=<span class="hljs-string">&quot;blue&quot;</span>, linewidth=<span class="hljs-number">1.00</span>)<br>ax.plot(x, x+<span class="hljs-number">4</span>, color=<span class="hljs-string">&quot;blue&quot;</span>, linewidth=<span class="hljs-number">2.00</span>)<br><br><span class="hljs-comment"># 虚线类型</span><br>ax.plot(x, x+<span class="hljs-number">5</span>, color=<span class="hljs-string">&quot;red&quot;</span>, lw=<span class="hljs-number">2</span>, linestyle=<span class="hljs-string">&#x27;-&#x27;</span>)<br>ax.plot(x, x+<span class="hljs-number">6</span>, color=<span class="hljs-string">&quot;red&quot;</span>, lw=<span class="hljs-number">2</span>, ls=<span class="hljs-string">&#x27;-.&#x27;</span>)<br>ax.plot(x, x+<span class="hljs-number">7</span>, color=<span class="hljs-string">&quot;red&quot;</span>, lw=<span class="hljs-number">2</span>, ls=<span class="hljs-string">&#x27;:&#x27;</span>)<br><br><span class="hljs-comment"># 虚线交错宽度</span><br>line, = ax.plot(x, x+<span class="hljs-number">8</span>, color=<span class="hljs-string">&quot;black&quot;</span>, lw=<span class="hljs-number">1.50</span>)<br>line.set_dashes([<span class="hljs-number">5</span>, <span class="hljs-number">10</span>, <span class="hljs-number">15</span>, <span class="hljs-number">10</span>])<br><br><span class="hljs-comment"># 符号</span><br>ax.plot(x, x + <span class="hljs-number">9</span>, color=<span class="hljs-string">&quot;green&quot;</span>, lw=<span class="hljs-number">2</span>, ls=<span class="hljs-string">&#x27;--&#x27;</span>, marker=<span class="hljs-string">&#x27;+&#x27;</span>)<br>ax.plot(x, x+<span class="hljs-number">10</span>, color=<span class="hljs-string">&quot;green&quot;</span>, lw=<span class="hljs-number">2</span>, ls=<span class="hljs-string">&#x27;--&#x27;</span>, marker=<span class="hljs-string">&#x27;o&#x27;</span>)<br>ax.plot(x, x+<span class="hljs-number">11</span>, color=<span class="hljs-string">&quot;green&quot;</span>, lw=<span class="hljs-number">2</span>, ls=<span class="hljs-string">&#x27;--&#x27;</span>, marker=<span class="hljs-string">&#x27;s&#x27;</span>)<br>ax.plot(x, x+<span class="hljs-number">12</span>, color=<span class="hljs-string">&quot;green&quot;</span>, lw=<span class="hljs-number">2</span>, ls=<span class="hljs-string">&#x27;--&#x27;</span>, marker=<span class="hljs-string">&#x27;1&#x27;</span>)<br><br><span class="hljs-comment"># 符号大小和颜色</span><br>ax.plot(x, x+<span class="hljs-number">13</span>, color=<span class="hljs-string">&quot;purple&quot;</span>, lw=<span class="hljs-number">1</span>, ls=<span class="hljs-string">&#x27;-&#x27;</span>, marker=<span class="hljs-string">&#x27;o&#x27;</span>, markersize=<span class="hljs-number">2</span>)<br>ax.plot(x, x+<span class="hljs-number">14</span>, color=<span class="hljs-string">&quot;purple&quot;</span>, lw=<span class="hljs-number">1</span>, ls=<span class="hljs-string">&#x27;-&#x27;</span>, marker=<span class="hljs-string">&#x27;o&#x27;</span>, markersize=<span class="hljs-number">4</span>)<br>ax.plot(x, x+<span class="hljs-number">15</span>, color=<span class="hljs-string">&quot;purple&quot;</span>, lw=<span class="hljs-number">1</span>, ls=<span class="hljs-string">&#x27;-&#x27;</span>,<br>     marker=<span class="hljs-string">&#x27;o&#x27;</span>, markersize=<span class="hljs-number">8</span>, markerfacecolor=<span class="hljs-string">&quot;red&quot;</span>)<br>   ax.plot(x, x+<span class="hljs-number">16</span>, color=<span class="hljs-string">&quot;purple&quot;</span>, lw=<span class="hljs-number">1</span>, ls=<span class="hljs-string">&#x27;-&#x27;</span>, marker=<span class="hljs-string">&#x27;s&#x27;</span>, markersize=<span class="hljs-number">8</span>,<br>     markerfacecolor=<span class="hljs-string">&quot;yellow&quot;</span>, markeredgewidth=<span class="hljs-number">2</span>, markeredgecolor=<span class="hljs-string">&quot;blue&quot;</span>)<br></code></pre></td></tr></table></figure></blockquote><h3 id="画布网格、坐标轴范围"><a href="#画布网格、坐标轴范围" class="headerlink" title="画布网格、坐标轴范围"></a>画布网格、坐标轴范围</h3><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs python">fig, axes = plt.subplots(<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, figsize=(<span class="hljs-number">10</span>, <span class="hljs-number">5</span>))<br><br><span class="hljs-comment"># 显示网格</span><br>axes[<span class="hljs-number">0</span>].plot(x, x**<span class="hljs-number">2</span>, x, x**<span class="hljs-number">3</span>, lw=<span class="hljs-number">2</span>)<br>axes[<span class="hljs-number">0</span>].grid(<span class="hljs-literal">True</span>,linestyle=<span class="hljs-string">&quot;--&quot;</span>,alpha=<span class="hljs-number">0.5</span>)<br><br><span class="hljs-comment"># 设置坐标轴范围</span><br>axes[<span class="hljs-number">1</span>].plot(x, x**<span class="hljs-number">2</span>, x, x**<span class="hljs-number">3</span>)<br>axes[<span class="hljs-number">1</span>].set_ylim([<span class="hljs-number">0</span>, <span class="hljs-number">60</span>])<br>axes[<span class="hljs-number">1</span>].set_xlim([<span class="hljs-number">2</span>, <span class="hljs-number">5</span>])<br></code></pre></td></tr></table></figure><p><code>除了折线图，Matplotlib 还支持绘制散点图、柱状图等其他常见图形。下面，我们绘制由散点图、梯步图、条形图、面积图构成的子图。</code></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs python">n = np.array([<span class="hljs-number">0</span>, <span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>, <span class="hljs-number">4</span>, <span class="hljs-number">5</span>])<br><br>fig, axes = plt.subplots(<span class="hljs-number">1</span>, <span class="hljs-number">4</span>, figsize=(<span class="hljs-number">16</span>, <span class="hljs-number">5</span>))<br><br>axes[<span class="hljs-number">0</span>].scatter(x, x + <span class="hljs-number">0.25</span>*np.random.randn(<span class="hljs-built_in">len</span>(x)))<br>axes[<span class="hljs-number">0</span>].set_title(<span class="hljs-string">&quot;scatter&quot;</span>)<br><br>axes[<span class="hljs-number">1</span>].step(n, n**<span class="hljs-number">2</span>, lw=<span class="hljs-number">2</span>)<br>axes[<span class="hljs-number">1</span>].set_title(<span class="hljs-string">&quot;step&quot;</span>)<br><br>axes[<span class="hljs-number">2</span>].bar(n, n**<span class="hljs-number">2</span>, align=<span class="hljs-string">&quot;center&quot;</span>, width=<span class="hljs-number">0.5</span>, alpha=<span class="hljs-number">0.5</span>)<br>axes[<span class="hljs-number">2</span>].set_title(<span class="hljs-string">&quot;bar&quot;</span>)<br><br>axes[<span class="hljs-number">3</span>].fill_between(x, x**<span class="hljs-number">2</span>, x**<span class="hljs-number">3</span>, color=<span class="hljs-string">&quot;green&quot;</span>, alpha=<span class="hljs-number">0.5</span>)<br>axes[<span class="hljs-number">3</span>].set_title(<span class="hljs-string">&quot;fill_between&quot;</span>)<br></code></pre></td></tr></table></figure></blockquote><h3 id="图形标注"><a href="#图形标注" class="headerlink" title="图形标注"></a>图形标注</h3><blockquote><p>Matplotlib 中，文字标注的方法由 <code>matplotlib.pyplot.text()</code> 实现。最基本的样式为 <code>matplotlib.pyplot.text(x, y, s)</code>，其中 x, y 用于标注位置定位，s 代表标注的字符串。除此之外，你还可以通过 <code>fontsize=</code> , <code>horizontalalignment=</code> 等参数调整标注字体的大小，对齐样式等。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs pyhton">fig, axes &#x3D; plt.subplots()<br><br>x_bar &#x3D; [10, 20, 30, 40, 50]  # 柱形图横坐标<br>y_bar &#x3D; [0.5, 0.6, 0.3, 0.4, 0.8]  # 柱形图纵坐标<br>bars &#x3D; axes.bar(x_bar, y_bar, color&#x3D;&#39;blue&#39;, label&#x3D;x_bar, width&#x3D;2)  # 绘制柱形图<br>for i, rect in enumerate(bars):<br>   x_text &#x3D; rect.get_x()  # 获取柱形图横坐标<br>   y_text &#x3D; rect.get_height() + 0.01  # 获取柱子的高度并增加 0.01<br>   plt.text(x_text, y_text, &#39;%.1f&#39; % y_bar[i])  # 标注文字<br></code></pre></td></tr></table></figure></blockquote><blockquote><p>除了文字标注之外，还可以通过 <code>matplotlib.pyplot.annotate()</code> 方法向图像中添加箭头等样式标注。接下来，我们向上面的例子中增添一行增加箭头标记的代码。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs python">fig, axes = plt.subplots()<br><br>bars = axes.bar(x_bar, y_bar, color=<span class="hljs-string">&#x27;blue&#x27;</span>, label=x_bar, width=<span class="hljs-number">2</span>)  <span class="hljs-comment"># 绘制柱形图</span><br><span class="hljs-keyword">for</span> i, rect <span class="hljs-keyword">in</span> <span class="hljs-built_in">enumerate</span>(bars):<br>    x_text = rect.get_x()  <span class="hljs-comment"># 获取柱形图横坐标</span><br>    y_text = rect.get_height() + <span class="hljs-number">0.01</span>  <span class="hljs-comment"># 获取柱子的高度并增加 0.01</span><br>    plt.text(x_text, y_text, <span class="hljs-string">&#x27;%.1f&#x27;</span> % y_bar[i])  <span class="hljs-comment"># 标注文字</span><br><br>    <span class="hljs-comment"># 增加箭头标注</span><br>    plt.annotate(<span class="hljs-string">&#x27;Min&#x27;</span>, xy=(<span class="hljs-number">32</span>, <span class="hljs-number">0.3</span>), xytext=(<span class="hljs-number">36</span>, <span class="hljs-number">0.3</span>),<br>                 arrowprops=<span class="hljs-built_in">dict</span>(facecolor=<span class="hljs-string">&#x27;black&#x27;</span>, width=<span class="hljs-number">1</span>, headwidth=<span class="hljs-number">7</span>))<br></code></pre></td></tr></table></figure><p><code>xy=()</code> 表示标注终点坐标，<code>xytext=()</code> 表示标注起点坐标。在箭头绘制的过程中，<code>arrowprops=()</code> 用于设置箭头样式，<code>facecolor=</code> 设置颜色，<code>width=</code> 设置箭尾宽度，<code>headwidth=</code> 设置箭头宽度，可以通过 <code>arrowstyle=</code> 改变箭头的样式。</p></blockquote><h1 id="常见图形绘制"><a href="#常见图形绘制" class="headerlink" title="常见图形绘制"></a>常见图形绘制</h1><blockquote><p><strong>折线图</strong>：以折线的上升或下降来表示统计数量的增减变化的统计图</p><p><strong>特点：能够显示数据的变化趋势，反映事物的变化情况。(变化)</strong></p><p>api：plt.plot(x, y)</p><p><strong>散点图：</strong>用两组数据构成多个坐标点，考察坐标点的分布,判断两变量之间是否存在某种关联或总结坐标点的分布模式。</p><p><strong>特点：判断变量之间是否存在数量关联趋势,展示离群点(分布规律)</strong></p><p>api：plt.scatter(x, y)</p><p><strong>柱状图：</strong>排列在工作表的列或行中的数据可以绘制到柱状图中。</p><p><strong>特点：绘制连离散的数据,能够一眼看出各个数据的大小,比较数据之间的差别。(统计/对比)</strong></p><p>api：plt.bar(x, width, align=’center’, **kwargs)</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs plain">Parameters:    <br>x : 需要传递的数据<br>width : 柱状图的宽度<br>align : 每个柱状图的位置对齐方式<br>    &#123;‘center’, ‘edge’&#125;, optional, default: ‘center’<br>**kwargs :<br>color:选择柱状图的颜色<br></code></pre></td></tr></table></figure><p><strong>直方图：</strong>由一系列高度不等的纵向条纹或线段表示数据分布的情况。 一般用横轴表示数据范围，纵轴表示分布情况。</p><p><strong>特点：绘制连续性的数据展示一组或者多组数据的分布状况(统计)</strong></p><p>api：matplotlib.pyplot.hist(x, bins=None)</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs plain">Parameters:    <br>x : 需要传递的数据<br>bins : 组距<br></code></pre></td></tr></table></figure><p><strong>饼图：</strong>用于表示不同分类的占比情况，通过弧度大小来对比各种分类。</p><p><strong>特点：分类数据的占比情况(占比)</strong></p><p>api：plt.pie(x, labels=,autopct=,colors)</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs plain">Parameters:  <br>x:数量，自动算百分比<br>labels:每部分名称<br>autopct:占比显示指定%1.2f%%<br>colors:每部分颜色<br></code></pre></td></tr></table></figure></blockquote>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>Pandas</title>
      <link href="2020/12/17/Pandas/"/>
      <url>2020/12/17/Pandas/</url>
      
        <content type="html"><![CDATA[<h1 id="Pandas"><a href="#Pandas" class="headerlink" title="Pandas"></a>Pandas</h1><blockquote><p><strong>Pandas优势</strong></p><p><strong>增强图表可读性</strong></p><p><strong>便捷的数据处理能力</strong></p><p><strong>读取文件方便</strong></p><p><strong>封装了Matplotlib、Numpy的画图和计算</strong></p></blockquote><h1 id="数据类型"><a href="#数据类型" class="headerlink" title="数据类型"></a>数据类型</h1><blockquote><p>Pandas的数据类型主要有：Series（一维数组），DataFrame（二维数组），Panel（三维数组），PanelND（N维数组）</p></blockquote><h2 id="Series"><a href="#Series" class="headerlink" title="Series"></a>Series</h2><blockquote><p>Series是Pandas中最基本的一维数组，可以存储整数、浮点数、字符串等数据。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">pandas.Series(data=<span class="hljs-literal">None</span>,index=<span class="hljs-literal">None</span>)<br></code></pre></td></tr></table></figure><ol><li><code>data</code>可以是字典，或者Numpy中的ndarray</li><li><code>index</code>是数据索引</li></ol><p>例：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">s = pandas.Series(&#123;<span class="hljs-string">&#x27;a&#x27;</span>: <span class="hljs-number">10</span>, <span class="hljs-string">&#x27;b&#x27;</span>: <span class="hljs-number">20</span>, <span class="hljs-string">&#x27;c&#x27;</span>: <span class="hljs-number">30</span>&#125;)<br>s = pandas.Series(np.random.randn(<span class="hljs-number">5</span>))<br></code></pre></td></tr></table></figure><p><strong>Series的属性</strong></p><p><strong>index：索引</strong></p><p><strong>values：数据</strong></p></blockquote><h2 id="DataFrame"><a href="#DataFrame" class="headerlink" title="DataFrame"></a>DataFrame</h2><h3 id="DataFrame的创建"><a href="#DataFrame的创建" class="headerlink" title="DataFrame的创建"></a>DataFrame的创建</h3><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">pandas.DataFrame(data=<span class="hljs-literal">None</span>, index=<span class="hljs-literal">None</span>, columns=<span class="hljs-literal">None</span>)<br></code></pre></td></tr></table></figure><p>区别于Series，其增加了columns索引。</p><ul><li>一维数组、列表、字典或者 Series 字典。</li><li>二维或者结构化的 <code>numpy.ndarray</code>。</li><li>一个 Series 或者另一个 DataFrame。</li></ul><p>例：</p><ol><li><p>使用由Series组成的字典来构建</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">pd.DataFrame(&#123;<span class="hljs-string">&#x27;one&#x27;</span>: pd.Series([<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>]),<br>              <span class="hljs-string">&#x27;two&#x27;</span>: pd.Series([<span class="hljs-number">4</span>, <span class="hljs-number">5</span>, <span class="hljs-number">6</span>])&#125;)<br></code></pre></td></tr></table></figure></li><li><p>直接通过列表创建</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">df = pd.DataFrame(&#123;<span class="hljs-string">&#x27;one&#x27;</span>: [<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>],<br>                   <span class="hljs-string">&#x27;two&#x27;</span>: [<span class="hljs-number">4</span>, <span class="hljs-number">5</span>, <span class="hljs-number">6</span>]&#125;)<br></code></pre></td></tr></table></figure></li><li><p>由带字典的列表创建</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python">df = pd.DataFrame([&#123;<span class="hljs-string">&#x27;one&#x27;</span>: <span class="hljs-number">1</span>, <span class="hljs-string">&#x27;two&#x27;</span>: <span class="hljs-number">4</span>&#125;,<br>                   &#123;<span class="hljs-string">&#x27;one&#x27;</span>: <span class="hljs-number">2</span>, <span class="hljs-string">&#x27;two&#x27;</span>: <span class="hljs-number">5</span>&#125;,<br>                   &#123;<span class="hljs-string">&#x27;one&#x27;</span>: <span class="hljs-number">3</span>, <span class="hljs-string">&#x27;two&#x27;</span>: <span class="hljs-number">6</span>&#125;])<br></code></pre></td></tr></table></figure></li><li><p>基于二维数值来构建</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">pd.DataFrame(np.random.randint(<span class="hljs-number">5</span>, size=(<span class="hljs-number">2</span>, <span class="hljs-number">4</span>)))<br></code></pre></td></tr></table></figure></li></ol></blockquote><h3 id="DataFrame的属性"><a href="#DataFrame的属性" class="headerlink" title="DataFrame的属性"></a>DataFrame的属性</h3><p><strong>查看DataFrame属性</strong></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs python">DataFrame.head() <span class="hljs-comment"># 默认显示前5条</span><br>DataFrame.tail(<span class="hljs-number">7</span>) <span class="hljs-comment"># 指定显示后7条</span><br>DataFrame.describe()<span class="hljs-comment"># 对数据集进行概览，会输出每一列数据的计数、最大值、最小值</span><br>DataFrame.values <span class="hljs-comment"># 将DataFrame转换成Numpy数组</span><br>DataFrame.index <span class="hljs-comment"># 查看索引</span><br>DataFrame.columns <span class="hljs-comment"># 查看列名</span><br>DataFrame.shape <span class="hljs-comment"># 查看形状</span><br></code></pre></td></tr></table></figure><h3 id="DatatFrame索引的设置"><a href="#DatatFrame索引的设置" class="headerlink" title="DatatFrame索引的设置"></a>DatatFrame索引的设置</h3><p><strong>修改行列索引值</strong></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">df.index &#x3D; 新索引值<br></code></pre></td></tr></table></figure><p><strong>重设索引值</strong></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs plain">reset_index(drop&#x3D;False)<br> 1.设置新的下标索引<br> 2.drop:默认为False，不删除原来索引，如果为True,删除原来的索引值<br></code></pre></td></tr></table></figure><p><strong>以某列值设置为新的索引</strong></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs plain">set_index(keys, drop&#x3D;True)<br> 1.keys : 列索引名成或者列索引名称的列表<br> 2.drop : boolean, default True.当做新的索引，删除原来的列<br></code></pre></td></tr></table></figure><h2 id="MultiIndex与Panel"><a href="#MultiIndex与Panel" class="headerlink" title="MultiIndex与Panel"></a>MultiIndex与Panel</h2><h3 id="MultiIndex"><a href="#MultiIndex" class="headerlink" title="MultiIndex"></a>MultiIndex</h3><p>MultiIndex是三维的数据结构;</p><p>多级索引（也称层次化索引）是pandas的重要功能，可以在Series、DataFrame对象上拥有2个以及2个以上的索引。</p><blockquote><p><strong>multiIndex的特性</strong></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python">MultiIndex(levels=[[<span class="hljs-number">2012</span>, <span class="hljs-number">2013</span>, <span class="hljs-number">2014</span>], [<span class="hljs-number">1</span>, <span class="hljs-number">4</span>, <span class="hljs-number">7</span>, <span class="hljs-number">10</span>]],<br>           labels=[[<span class="hljs-number">0</span>, <span class="hljs-number">2</span>, <span class="hljs-number">1</span>, <span class="hljs-number">2</span>], [<span class="hljs-number">0</span>, <span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>]],<br>           names=[<span class="hljs-string">&#x27;year&#x27;</span>, <span class="hljs-string">&#x27;month&#x27;</span>])<br></code></pre></td></tr></table></figure><p>多级或分层索引对象</p><ul><li>index属性<ul><li>names:levels的名称</li><li>levels：每个level的元组值</li></ul></li></ul><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs python">df.index.names<br><span class="hljs-comment"># FrozenList([&#x27;year&#x27;, &#x27;month&#x27;])</span><br><br>df.index.levels<br><span class="hljs-comment"># FrozenList([[1, 2], [1, 4, 7, 10]])</span><br></code></pre></td></tr></table></figure></blockquote><blockquote><p><strong>multiIndex的创建</strong></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs python">arrays = [[<span class="hljs-number">1</span>, <span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">2</span>], [<span class="hljs-string">&#x27;red&#x27;</span>, <span class="hljs-string">&#x27;blue&#x27;</span>, <span class="hljs-string">&#x27;red&#x27;</span>, <span class="hljs-string">&#x27;blue&#x27;</span>]]<br>pd.MultiIndex.from_arrays(arrays, names=(<span class="hljs-string">&#x27;number&#x27;</span>, <span class="hljs-string">&#x27;color&#x27;</span>))<br><br><span class="hljs-comment"># 结果</span><br>MultiIndex(levels=[[<span class="hljs-number">1</span>, <span class="hljs-number">2</span>], [<span class="hljs-string">&#x27;blue&#x27;</span>, <span class="hljs-string">&#x27;red&#x27;</span>]],<br>           codes=[[<span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">1</span>, <span class="hljs-number">1</span>], [<span class="hljs-number">1</span>, <span class="hljs-number">0</span>, <span class="hljs-number">1</span>, <span class="hljs-number">0</span>]],<br>           names=[<span class="hljs-string">&#x27;number&#x27;</span>, <span class="hljs-string">&#x27;color&#x27;</span>])<br></code></pre></td></tr></table></figure></blockquote><h3 id="Panel"><a href="#Panel" class="headerlink" title="Panel"></a>Panel</h3><blockquote><p><strong>panel的创建</strong></p><p><code>pandas.Panel</code>(<em>data=None</em>, <em>items=None</em>, <em>major_axis=None</em>, <em>minor_axis=None</em>)</p><ul><li>作用：存储3维数组的Panel结构</li><li>参数：<ul><li><strong>data</strong> : ndarray或者dataframe</li><li><strong>items</strong> : 索引或类似数组的对象，axis=0</li><li><strong>major_axis</strong> : 索引或类似数组的对象，axis=1</li><li><strong>minor_axis</strong> : 索引或类似数组的对象，axis=2</li></ul></li></ul><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs python">p = pd.Panel(data=np.arange(<span class="hljs-number">24</span>).reshape(<span class="hljs-number">4</span>,<span class="hljs-number">3</span>,<span class="hljs-number">2</span>),<br>                 items=<span class="hljs-built_in">list</span>(<span class="hljs-string">&#x27;ABCD&#x27;</span>),<br>                 major_axis=pd.date_range(<span class="hljs-string">&#x27;20130101&#x27;</span>, periods=<span class="hljs-number">3</span>),<br>                 minor_axis=[<span class="hljs-string">&#x27;first&#x27;</span>, <span class="hljs-string">&#x27;second&#x27;</span>])<br></code></pre></td></tr></table></figure><p><strong>查看panel数据</strong></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs plain">p[:,:,&quot;first&quot;]<br>p[&quot;B&quot;,:,:]<br></code></pre></td></tr></table></figure><p><strong>注：Pandas从版本0.20.0开始弃用：推荐的用于表示3D数据的方法是通过DataFrame上的MultiIndex方法</strong></p></blockquote><h1 id="基本数据操作"><a href="#基本数据操作" class="headerlink" title="基本数据操作"></a>基本数据操作</h1><h2 id="数据索引"><a href="#数据索引" class="headerlink" title="数据索引"></a>数据索引</h2><h3 id="基于索引下标进行选择"><a href="#基于索引下标进行选择" class="headerlink" title="基于索引下标进行选择"></a>基于索引下标进行选择</h3><blockquote><p>Pandas中的<code>.iloc</code>方法可以基于数字索引对数据集进行选择，可接受的类型：</p><ol><li>整数。例如：<code>5</code></li><li>整数构成的列表或数组。例如：<code>[1, 2, 3]</code></li><li>布尔数组。</li><li>可返回索引值的函数或参数。</li></ol><p><code>df.iloc[]</code> 的 <code>[[行]，[列]]</code> 里面可以同时接受行和列的位置</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">&gt;df.iloc[[<span class="hljs-number">1</span>, <span class="hljs-number">3</span>, <span class="hljs-number">5</span>]] <span class="hljs-comment">#选择1，3，5行</span><br>df.iloc[:, <span class="hljs-number">1</span>:<span class="hljs-number">4</span>] <span class="hljs-comment">#选择2-4列</span><br></code></pre></td></tr></table></figure></blockquote><h3 id="基于标签名选择"><a href="#基于标签名选择" class="headerlink" title="基于标签名选择"></a>基于标签名选择</h3><blockquote><p>除了根据数字索引选择，还可以直接根据标签对应的名称选择。这里用到的方法和上面的 <code>iloc</code> 很相似，少了个 <code>i</code> 为 <code>df.loc[]</code>,可接受类型有：</p><ol><li>单个标签。例如：<code>2</code> 或 <code>&#39;a&#39;</code>，这里的 <code>2</code> 指的是标签而不是索引位置。</li><li>列表或数组包含的标签。例如：<code>[&#39;A&#39;, &#39;B&#39;, &#39;C&#39;]</code>。</li><li>切片对象。例如：<code>&#39;A&#39;:&#39;E&#39;</code>，注意这里和上面切片的不同之处，首尾都包含在内。</li><li>布尔数组。</li><li>可返回标签的函数或参数。</li></ol></blockquote><h3 id="组合索引"><a href="#组合索引" class="headerlink" title="组合索引"></a>组合索引</h3><blockquote><p>使用df.ix进行下标和名称组合索引</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">data.ix[<span class="hljs-number">0</span>:<span class="hljs-number">4</span>, [<span class="hljs-string">&#x27;open&#x27;</span>, <span class="hljs-string">&#x27;close&#x27;</span>, <span class="hljs-string">&#x27;high&#x27;</span>, <span class="hljs-string">&#x27;low&#x27;</span>]]<br></code></pre></td></tr></table></figure><p>推荐使用loc和iloc来获取的方式</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">data.loc[data.index[<span class="hljs-number">0</span>:<span class="hljs-number">4</span>], [<span class="hljs-string">&#x27;open&#x27;</span>, <span class="hljs-string">&#x27;close&#x27;</span>, <span class="hljs-string">&#x27;high&#x27;</span>, <span class="hljs-string">&#x27;low&#x27;</span>]]<br>data.iloc[<span class="hljs-number">0</span>:<span class="hljs-number">4</span>, data.columns.get_indexer([<span class="hljs-string">&#x27;open&#x27;</span>, <span class="hljs-string">&#x27;close&#x27;</span>, <span class="hljs-string">&#x27;high&#x27;</span>, <span class="hljs-string">&#x27;low&#x27;</span>])]<br></code></pre></td></tr></table></figure></blockquote><h3 id="直接进行索引"><a href="#直接进行索引" class="headerlink" title="直接进行索引"></a>直接进行索引</h3><blockquote><p><code>先列后行</code>，按照索引的字符串进行索引</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">df[列索引值][行索引值]<br></code></pre></td></tr></table></figure><p><strong>不可以先行后列</strong></p><p><strong>也不可以传索引下标</strong></p></blockquote><h2 id="数据赋值"><a href="#数据赋值" class="headerlink" title="数据赋值"></a>数据赋值</h2><blockquote><p>将close列赋值为1</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 直接修改原来的值</span><br>df[<span class="hljs-string">&#x27;close&#x27;</span>] = <span class="hljs-number">1</span><br><span class="hljs-comment"># 或者</span><br>df.close = <span class="hljs-number">1</span><br></code></pre></td></tr></table></figure></blockquote><h2 id="排序"><a href="#排序" class="headerlink" title="排序"></a>排序</h2><h3 id="DataFrame排序"><a href="#DataFrame排序" class="headerlink" title="DataFrame排序"></a>DataFrame排序</h3><blockquote><p>使用<code>df.sort_values(by=, ascending=)</code></p><ul><li>单个键或者多个键进行排序,</li><li>参数：<ul><li>by：指定排序参考的键，标签名或者列表</li><li>ascending:默认升序<ul><li>ascending=False:降序</li><li>ascending=True:升序</li></ul></li></ul></li></ul><p>使用<code>df.sort_index(axis=)</code>对指定轴索引进行排序，从小到大</p></blockquote><h3 id="Series排序"><a href="#Series排序" class="headerlink" title="Series排序"></a>Series排序</h3><blockquote><ul><li>使用<code>series.sort_values(ascending=True)</code>进行升序排序</li><li>使用<code>series.sort_index()</code>进行排序</li></ul></blockquote><h2 id="数据删减"><a href="#数据删减" class="headerlink" title="数据删减"></a>数据删减</h2><blockquote><ol><li><p><code>DataFrame.drop</code> 可以直接去掉数据集中指定的列和行。一般在使用时，我们指定 <code>labels</code> 标签参数，然后再通过 <code>axis</code> 指定按列或按行删除即可。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">df.drop(labels&#x3D;[&#39;Median Age&#39;, &#39;Total Males&#39;], axis&#x3D;1)<br></code></pre></td></tr></table></figure></li><li><p><code>DataFrame.drop_duplicates</code>则通常用于数据去重，即剔除数据集中的重复值。使用方法非常简单，指定去除重复值规则，以及 <code>axis</code> 按列还是按行去除即可。</p></li><li><p><code>DataFrame.dropna</code>也十分常用，其主要的用途是删除缺少值，即数据集中空缺的数据列或行。</p></li></ol></blockquote><h1 id="DataFrame运算"><a href="#DataFrame运算" class="headerlink" title="DataFrame运算"></a>DataFrame运算</h1><h2 id="算术运算"><a href="#算术运算" class="headerlink" title="算术运算"></a>算术运算</h2><blockquote><ol><li><p><code>add(other)</code>:加法运算</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">df[&#39;open&#39;].add(10)<br></code></pre></td></tr></table></figure></li><li><p><code>sub(other)</code>：减法运算</p></li></ol></blockquote><h2 id="逻辑运算"><a href="#逻辑运算" class="headerlink" title="逻辑运算"></a>逻辑运算</h2><blockquote><p><strong>逻辑运算符号</strong></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python">data[<span class="hljs-string">&quot;open&quot;</span>] &gt; <span class="hljs-number">23</span>返回逻辑结果，布尔值<br><span class="hljs-comment"># 逻辑判断的结果可以作为筛选的依据</span><br>data[data[<span class="hljs-string">&quot;open&quot;</span>] &gt; <span class="hljs-number">23</span>].head()<br></code></pre></td></tr></table></figure><p>完成多个逻辑判断</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">data[(data[&quot;open&quot;] &gt; 23) &amp; (data[&quot;open&quot;] &lt; 24)].head()<br></code></pre></td></tr></table></figure></blockquote><blockquote><p><strong>逻辑运算函数</strong></p><p><code>query(expr)</code></p><p><code>expr</code>:查询字符串</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">data.query(&quot;open&lt;24 &amp; open&gt;23&quot;).head()<br></code></pre></td></tr></table></figure><p><code>isin(values)</code></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 可以指定值进行一个判断，从而进行筛选操作</span><br>data[data[<span class="hljs-string">&quot;open&quot;</span>].isin([<span class="hljs-number">23.53</span>, <span class="hljs-number">23.85</span>])]<br></code></pre></td></tr></table></figure></blockquote><h2 id="统计运算"><a href="#统计运算" class="headerlink" title="统计运算"></a>统计运算</h2><blockquote><ol><li><p><code>describe</code></p><p>综合分析: 能够直接得出很多统计结果,<code>count</code>, <code>mean</code>, <code>std</code>, <code>min</code>, <code>max</code> 等</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 计算平均值、标准差、最大值、最小值</span><br>data.describe()<br></code></pre></td></tr></table></figure></li><li><p><code>统计函数</code>：</p><table><thead><tr><th>函数</th><th>意义</th></tr></thead><tbody><tr><td><code>sum</code></td><td>总和</td></tr><tr><td><code>mean</code></td><td>平均值</td></tr><tr><td><code>median</code></td><td>中位数</td></tr><tr><td><code>min</code></td><td>最小值</td></tr><tr><td><code>max</code></td><td>最大值</td></tr><tr><td><code>mode</code></td><td>众数</td></tr><tr><td><code>abs</code></td><td>绝对值</td></tr><tr><td><code>prod</code></td><td>连乘</td></tr><tr><td><code>std</code></td><td>标准差</td></tr><tr><td><code>var</code></td><td>方差</td></tr><tr><td><code>idxmax</code></td><td>最大值索引</td></tr><tr><td><code>idxmin</code></td><td>最小值索引</td></tr><tr><td><code>count</code></td><td>总样本数</td></tr></tbody></table></li></ol><p><strong>注意：对于单个函数去进行统计的时候，坐标轴还是按照默认列“columns” (axis=0, default)，如果要对行“index” 需要指定(axis=1)</strong></p><ol start="3"><li><p><code>累计统计函数:</code></p><table><thead><tr><th>函数</th><th>作用</th></tr></thead><tbody><tr><td><code>cumsum</code></td><td><strong>计算前1/2/3/…/n个数的和</strong></td></tr><tr><td><code>cummax</code></td><td>计算前1/2/3/…/n个数的最大值</td></tr><tr><td><code>cummin</code></td><td>计算前1/2/3/…/n个数的最小值</td></tr><tr><td><code>cumprod</code></td><td>计算前1/2/3/…/n个数的积</td></tr></tbody></table></li></ol><p><code>Dataframe.corr():</code>计算每两列之间的相关性</p></blockquote><h2 id="自定义运算"><a href="#自定义运算" class="headerlink" title="自定义运算"></a>自定义运算</h2><blockquote><p><code>apply(func, axis=0)</code></p><ul><li>func:自定义函数</li><li>axis=0:默认是列，axis=1为行进行运算</li></ul><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 定义一个对列，最大值-最小值的函数</span><br>data[[<span class="hljs-string">&#x27;open&#x27;</span>, <span class="hljs-string">&#x27;close&#x27;</span>]].apply(<span class="hljs-keyword">lambda</span> x: x.<span class="hljs-built_in">max</span>() - x.<span class="hljs-built_in">min</span>(), axis=<span class="hljs-number">0</span>)<br></code></pre></td></tr></table></figure></blockquote><h1 id="Pandas画图"><a href="#Pandas画图" class="headerlink" title="Pandas画图"></a>Pandas画图</h1><blockquote><ol><li><p><code>pandas.DataFrame.plot:</code></p><p><code>DataFrame.plot</code>(<em>kind=’line’</em>)</p><p><code>kind : str</code>:需要绘制图形的种类</p><ul><li>‘line’ : line plot (default)</li><li>‘bar’ : vertical bar plot</li><li>‘barh’ : horizontal bar plot</li><li>‘hist’ : histogram</li><li>‘pie’ : pie plot</li><li>‘scatter’ : scatter plot</li></ul></li><li><p><code>pandas.Series.plot</code></p></li></ol></blockquote><h1 id="文件读取与存储"><a href="#文件读取与存储" class="headerlink" title="文件读取与存储"></a>文件读取与存储</h1><h2 id="CSV"><a href="#CSV" class="headerlink" title="CSV"></a>CSV</h2><blockquote><p><code>pandas.read_csv(filepath_or_buffer, sep =&#39;,&#39;, usecols=[],engine=&#39;python&#39;,encoding=&#39;utf8&#39;):</code></p><ul><li>filepath_or_buffer:文件路径</li><li>sep :分隔符，默认用”,”隔开</li><li>usecols:指定读取的列名，列表形式</li></ul><p><code>DataFrame.to_csv(path_or_buf=None, sep=&#39;, ’, columns=None, header=True, index=True, mode=&#39;w&#39;, encoding=None):</code></p><ul><li>path_or_buf :文件路径</li><li>sep :分隔符，默认用”,”隔开</li><li>columns :选择需要的列索引,列表形式</li><li>header :是否写进列索引值</li><li>index:是否写进行行索引</li><li>mode:’w’：重写, ‘a’ 追加</li></ul></blockquote><h2 id="HDF5"><a href="#HDF5" class="headerlink" title="HDF5"></a>HDF5</h2><blockquote><p><code>pandas.read_hdf(path_or_buf，key=None，** kwargs):</code></p><ul><li>path_or_buffer:文件路径</li><li>key:读取的键</li><li>return:Theselected object</li></ul><p><code>DataFrame.to_hdf(path_or_buf, key=, **kwargs*)</code></p><p>查看HD5文件中的所有key值：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs python">pip install h5py<br><span class="hljs-keyword">import</span> h5py<br>f = h5py.File(<span class="hljs-string">&quot;path&quot;</span>,<span class="hljs-string">&quot;r&quot;</span>)<br>f.keys()<br></code></pre></td></tr></table></figure></blockquote><h2 id="JSON"><a href="#JSON" class="headerlink" title="JSON"></a>JSON</h2><blockquote><p><code>pandas.read_json(path_or_buf=None, orient=None, typ=&#39;frame&#39;, lines=False):</code></p><p>pandas.read_json(path_or_buf=None, orient=None, typ=’frame’, lines=False)</p><ul><li>将JSON格式准换成默认的Pandas DataFrame格式</li><li>orient : JSON string format<ul><li>‘split’ : dict like {index -&gt; [index], columns -&gt; [columns], data -&gt; [values]}<ul><li>split 将索引总结到索引，列名到列名，数据到数据。将三部分都分开了</li></ul></li><li>‘records’ : list like [{column -&gt; value}, … , {column -&gt; value}]<ul><li>records 以<code>columns：values</code>的形式输出</li></ul></li><li>‘index’ : dict like {index -&gt; {column -&gt; value}}<ul><li>index 以<code>index：&#123;columns：values&#125;...</code>的形式输出</li></ul></li><li>‘columns’ : dict like {column -&gt; {index -&gt; value}},默认该格式<ul><li>colums 以<code>columns:&#123;index:values&#125;</code>的形式输出</li></ul></li><li>‘values’ : just the values array<ul><li>values 直接输出值</li></ul></li></ul></li><li>lines : 默认False<ul><li>按照每行读取json对象</li></ul></li><li>typ : 默认‘frame’， 指定转换成的对象类型series或者dataframe</li></ul><p><code>DataFrame.to_json(path_or_buf=None, orient=None, lines=False):</code></p><ul><li>将Pandas 对象存储为json格式</li><li><em>path_or_buf=None</em>：文件地址</li><li>orient:存储的json形式，{‘split’,’records’,’index’,’columns’,’values’}</li><li>lines:一个对象存储为一行</li></ul></blockquote><h1 id="高级处理"><a href="#高级处理" class="headerlink" title="高级处理"></a>高级处理</h1><h2 id="缺失值的处理"><a href="#缺失值的处理" class="headerlink" title="缺失值的处理"></a>缺失值的处理</h2><blockquote><p>Pandas 为了更方便地检测缺失值，将不同类型数据的缺失均采用 <code>NaN</code> 标记。这里的 NaN 代表 Not a Number，它仅仅是作为一个标记。例外是，在时间序列里，时间戳的丢失采用 <code>NaT</code> 标记。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">np.<span class="hljs-built_in">all</span>(pd.notnull(df)) <span class="hljs-comment"># 没有缺失值返回True，有返回False</span><br>np.<span class="hljs-built_in">any</span>(pd.isnull(df)) <span class="hljs-comment"># 没有缺失值返回False，有返回True</span><br></code></pre></td></tr></table></figure><p>Pandas 中用于检测缺失值主要用到两个方法，分别是：<code>isna()</code>和 <code>notna()</code>。</p><ol><li><p>存在缺失值NaN</p><ul><li>删除</li></ul><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 不修改原数据</span><br>movie.dropna()<br><span class="hljs-comment"># 可以定义新的变量接受或者用原来的变量名</span><br>data = movie.dropna()<br></code></pre></td></tr></table></figure><ul><li>替换缺失值</li></ul><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 替换存在缺失值的样本的两列</span><br><span class="hljs-comment"># 替换填充平均值，中位数</span><br>movie[<span class="hljs-string">&#x27;Revenue (Millions)&#x27;</span>].fillna(movie[<span class="hljs-string">&#x27;Revenue (Millions)&#x27;</span>].mean(), inplace=<span class="hljs-literal">True</span>)<br></code></pre></td></tr></table></figure><p>​    b. 替换所有缺失值</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> movie.columns:<br>    <span class="hljs-keyword">if</span> np.<span class="hljs-built_in">all</span>(pd.notnull(movie[i])) == <span class="hljs-literal">False</span>:<br>        <span class="hljs-built_in">print</span>(i)<br>        movie[i].fillna(movie[i].mean(), inplace=<span class="hljs-literal">True</span>)<br></code></pre></td></tr></table></figure></li><li><p>不是缺失值nan，有默认标记的</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-comment"># 全局取消证书验证</span><br><span class="hljs-keyword">import</span> ssl<br>ssl._create_default_https_context = ssl._create_unverified_context<br></code></pre></td></tr></table></figure><p>先替换‘?’为np.nan</p><ul><li><code>df.replace(to_replace=&quot;?&quot;, value=np.nan)</code><ul><li>to_replace:替换前的值</li><li>value:替换后的值</li></ul></li></ul></li></ol><p><code>缺失值填充</code>：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs python">df.fillna(<span class="hljs-number">0</span>) <span class="hljs-comment"># 用0替换缺失值</span><br>df.fillna(method=<span class="hljs-string">&#x27;pad&#x27;</span>) <span class="hljs-comment"># 使用缺失值前面的值进行填充</span><br>df.fillna(method=<span class="hljs-string">&#x27;bfill&#x27;</span>) <span class="hljs-comment"># 使用缺失值前面的值进行填充</span><br>df.fillna(method=<span class="hljs-string">&#x27;pad&#x27;</span>, limit=<span class="hljs-number">1</span>)  <span class="hljs-comment"># 最多填充一项</span><br>df.fillna(df.mean()[<span class="hljs-string">&#x27;C&#x27;</span>:<span class="hljs-string">&#x27;E&#x27;</span>]) <span class="hljs-comment">#对C列到E列用平均值填充</span><br></code></pre></td></tr></table></figure></blockquote><h2 id="插值填充"><a href="#插值填充" class="headerlink" title="插值填充"></a>插值填充</h2><blockquote><p>插值是数值分析中一种方法。简而言之，就是借助于一个函数（线性或非线性），再根据已知数据去求解未知数据的值。插值在数据领域非常常见，它的好处在于，可以尽量去还原数据本身的样子。</p><p>可以通过<code>interpolate</code>方法完成线性插值。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python">df = pd.DataFrame(&#123;<span class="hljs-string">&#x27;A&#x27;</span>: [<span class="hljs-number">1.1</span>, <span class="hljs-number">2.2</span>, np.nan, <span class="hljs-number">4.5</span>, <span class="hljs-number">5.7</span>, <span class="hljs-number">6.9</span>],<br>                <span class="hljs-string">&#x27;B&#x27;</span>: [<span class="hljs-number">.21</span>, np.nan, np.nan, <span class="hljs-number">3.1</span>, <span class="hljs-number">11.7</span>, <span class="hljs-number">13.2</span>]&#125;)<br>df_interpolate = df.interpolate()<br></code></pre></td></tr></table></figure><p>对于 <code>interpolate()</code>支持的插值算法，也就是 <code>method=</code></p><ol><li>如果你的数据增长速率越来越快，可以选择 <code>method=&#39;quadratic&#39;</code>二次插值。</li><li>如果数据集呈现出累计分布的样子，推荐选择 <code>method=&#39;pchip&#39;</code>。</li><li>如果需要填补缺省值，以平滑绘图为目标，推荐选择 <code>method=&#39;akima&#39;</code>。</li></ol><p>需要安装了Scipy库。</p></blockquote><h2 id="数据离散化"><a href="#数据离散化" class="headerlink" title="数据离散化"></a>数据离散化</h2><blockquote><p>连续属性离散化的目的是为了简化数据结构，<strong>数据离散化技术可以用来减少给定连续属性值的个数</strong>。离散化方法经常作为数据挖掘的工具。</p><p><strong>连续属性的离散化就是在连续属性的值域上，将值域划分为若干个离散的区间，最后用不同的符号或整数</strong> <strong>值代表落在每个子区间中的属性值。</strong></p><p><strong>自动分组：</strong></p><ul><li><code>pd.qcut(data, num)：</code>将数据分为差不多数量的几组,一般会与value_counts搭配使用，统计每组的个数，num为组数</li><li><code>series.value_counts()：</code>统计分组次数</li></ul><p><strong>自定义分组：</strong></p><ul><li><code>pd.cut(data, bins):</code><ul><li>bins参数为自己定义的分组区间，用列表</li></ul></li></ul><p><strong>分组数据变成one-hot编码：</strong></p><p><code>pandas.get_dummies(data, prefix=None)</code></p><ul><li>data:array-like, Series, or DataFrame</li><li>prefix:分组名字</li></ul></blockquote><h2 id="数据合并"><a href="#数据合并" class="headerlink" title="数据合并"></a>数据合并</h2><blockquote><ol><li><code>pd.concat([data1, data2], axis=1)</code><ul><li>按照行或列进行合并,axis=0为列索引，axis=1为行索引</li></ul></li><li><code>pd.merge(left, right, how=&#39;inner&#39;, on=[]):</code><ul><li>可以指定按照两组数据的共同键值对合并或者左右各自</li><li><code>left</code>: DataFrame</li><li><code>right</code>: 另一个DataFrame</li><li><code>on</code>: 指定的共同键，列表</li><li><code>how</code>:按照什么方式连接，left，right，outer，inner,默认inner</li></ul></li></ol></blockquote><h2 id="交叉表和透视表"><a href="#交叉表和透视表" class="headerlink" title="交叉表和透视表"></a>交叉表和透视表</h2><blockquote><ul><li><p>交叉表：</p><p>交叉表用于计算一列数据对于另外一列数据的分组个数(用于统计分组频率的特殊透视表)</p><ul><li><p><code>pd.crosstab(index,columns)</code></p><p>按index进行分组并作为行索引，并统计columns值的各个频数</p></li></ul></li><li><p>透视表：</p><p>透视表是将原有的DataFrame的列分别作为行索引和列索引，然后对指定的列应用聚集函数</p><ul><li><p><code>DataFrame.pivot_table(values=[], index=[],aggfunc=np.mean)</code></p><p>将values按照index分组，默认使用mean()聚合函数</p></li></ul></li></ul></blockquote><h2 id="分组与聚合"><a href="#分组与聚合" class="headerlink" title="分组与聚合"></a>分组与聚合</h2><blockquote><p><strong>分组：</strong></p><p><code>DataFrame.groupby(key, as_index=False)</code></p><p>key:分组的列数据，可以多个</p><p><strong>聚合：</strong></p><p><code>agg(&#123;&quot;列名&quot;：np.聚合函数&#125;)</code></p><p><code>max(),min(),mean(),std()</code>等函数</p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 科学计算库 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Numpy</title>
      <link href="2020/12/15/Numpy/"/>
      <url>2020/12/15/Numpy/</url>
      
        <content type="html"><![CDATA[<h1 id="Numpy数组类型"><a href="#Numpy数组类型" class="headerlink" title="Numpy数组类型"></a>Numpy数组类型</h1><blockquote><p>Numpy的优势：</p><ol><li><p><strong>内存风格</strong></p><p>ndarray在存储数据的时候，数据与数据的地址都是连续的，这样就给使得批量操作数组元素时速度更快。</p></li><li><p><strong>ndarray支持并行化运算（向量化运算）</strong></p><p>numpy内置了并行运算功能，当系统有多个核心时，做某种计算时，numpy会自动做并行计算</p></li><li><p><strong>效率远高于纯Python代码</strong></p><p>Numpy底层使用C语言编写，内部解除了GIL（全局解释器锁），其对数组的操作速度不受Python解释器的限制，所以，其效率远高于纯Python代码。</p></li></ol></blockquote><table><thead><tr><th align="center">类型</th><th align="center">解释</th></tr></thead><tbody><tr><td align="center">bool</td><td align="center">布尔类型，1 个字节，值为 True 或 False。</td></tr><tr><td align="center">int</td><td align="center">整数类型，通常为 int64 或 int32 。</td></tr><tr><td align="center">intc</td><td align="center">与 C 里的 int 相同，通常为 int32 或 int64。</td></tr><tr><td align="center">intp</td><td align="center">用于索引，通常为 int32 或 int64。</td></tr><tr><td align="center">int8</td><td align="center">字节（从 -128 到 127）</td></tr><tr><td align="center">int16</td><td align="center">整数（从 -32768 到 32767）</td></tr><tr><td align="center">int32</td><td align="center">整数（从 -2147483648 到 2147483647）</td></tr><tr><td align="center">int64</td><td align="center">整数（从 -9223372036854775808 到 9223372036854775807）</td></tr><tr><td align="center">uint8</td><td align="center">无符号整数（从 0 到 255）</td></tr><tr><td align="center">uint16</td><td align="center">无符号整数（从 0 到 65535）</td></tr><tr><td align="center">uint32</td><td align="center">无符号整数（从 0 到 4294967295）</td></tr><tr><td align="center">uint64</td><td align="center">无符号整数（从 0 到 18446744073709551615）</td></tr><tr><td align="center">float</td><td align="center">float64 的简写。</td></tr><tr><td align="center">float16</td><td align="center">半精度浮点，5 位指数，10 位尾数</td></tr><tr><td align="center">float32</td><td align="center">单精度浮点，8 位指数，23 位尾数</td></tr><tr><td align="center">float64</td><td align="center">双精度浮点，11 位指数，52 位尾数</td></tr><tr><td align="center">complex</td><td align="center">complex128 的简写。</td></tr><tr><td align="center">complex64</td><td align="center">复数，由两个 32 位浮点表示。</td></tr><tr><td align="center">complex128</td><td align="center">复数，由两个 64 位浮点表示。</td></tr><tr><td align="center">object_</td><td align="center">python对象</td></tr><tr><td align="center">string_</td><td align="center">字符串</td></tr><tr><td align="center">unicode_</td><td align="center">unicode类型</td></tr></tbody></table><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">import</span> numpy <span class="hljs-keyword">as</span> np<br>a = np.array([<span class="hljs-number">1.1</span>,<span class="hljs-number">2.2</span>,<span class="hljs-number">3.3</span>],dtype=np.float64)<br>a.astype(<span class="hljs-built_in">int</span>).dtype <span class="hljs-comment"># 将a的数值类型从float64转为int64</span><br></code></pre></td></tr></table></figure></blockquote><h1 id="Numpy数组生成"><a href="#Numpy数组生成" class="headerlink" title="Numpy数组生成"></a>Numpy数组生成</h1><blockquote><p>Numpy中，ndarray多维数组对象有六个参数：</p><ul><li>shape：数组的形状</li><li>dtype：数组类型</li><li>buffer：对象暴露缓冲区接口</li><li>offset：数组数据的偏移量</li><li>strides：数据步长</li><li>order：{‘C’,’F’},以行或列为主排列顺序</li></ul></blockquote><h2 id="列表或元组转换"><a href="#列表或元组转换" class="headerlink" title="列表或元组转换"></a>列表或元组转换</h2><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python">numpy.array(<span class="hljs-built_in">object</span>,dtype=<span class="hljs-literal">None</span>,copy=<span class="hljs-literal">True</span>,order=<span class="hljs-literal">None</span>,subok=<span class="hljs-literal">False</span>,ndmin=<span class="hljs-number">0</span>)<br>a1 = numpy.array(a) <span class="hljs-comment"># 深拷贝</span><br>a2 = numpy.asarray(a) <span class="hljs-comment"># 浅拷贝</span><br></code></pre></td></tr></table></figure><blockquote><p>其中参数：</p><ol><li><code>object</code>：列表、元组等。</li><li><code>dtype</code>：数据类型。如果未给出，则类型为被保存对象所需的最小类型。</li><li><code>copy</code>：布尔类型，默认 True，表示复制对象。</li><li><code>order</code>：顺序。</li><li><code>subok</code>：布尔类型，表示子类是否被传递。</li><li><code>ndmin</code>：生成的数组应具有的最小维数。</li></ol></blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">numpy.array([[<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>], [<span class="hljs-number">4</span>, <span class="hljs-number">5</span>, <span class="hljs-number">6</span>]])<br>numpy.array([(<span class="hljs-number">1</span>, <span class="hljs-number">2</span>), (<span class="hljs-number">3</span>, <span class="hljs-number">4</span>), (<span class="hljs-number">5</span>, <span class="hljs-number">6</span>)])<br></code></pre></td></tr></table></figure><h2 id="numpy原生方法创建"><a href="#numpy原生方法创建" class="headerlink" title="numpy原生方法创建"></a>numpy原生方法创建</h2><h3 id="arange方法创建"><a href="#arange方法创建" class="headerlink" title="arange方法创建"></a>arange方法创建</h3><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">numpy.arange(start,stop,step,dtype=<span class="hljs-literal">None</span>)<br>例：numpy.arange(<span class="hljs-number">3</span>,<span class="hljs-number">7</span>,<span class="hljs-number">0.5</span>,dtype=<span class="hljs-string">&#x27;float32&#x27;</span>)<br></code></pre></td></tr></table></figure><blockquote><p>arange()的功能是在给定区间内创建一系列均匀间隔的值，指定[开始，停止),并指定步长step</p></blockquote><h3 id="linspace方法创建"><a href="#linspace方法创建" class="headerlink" title="linspace方法创建"></a>linspace方法创建</h3><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">numpy.linspace(start, stop, num=<span class="hljs-number">50</span>, endpoint=<span class="hljs-literal">True</span>, retstep=<span class="hljs-literal">False</span>, dtype=<span class="hljs-literal">None</span>)<br>例：numpy.linspace(<span class="hljs-number">0</span>, <span class="hljs-number">10</span>, <span class="hljs-number">10</span>, endpoint=<span class="hljs-literal">True</span>)<br></code></pre></td></tr></table></figure><blockquote><p>同arange一样，用于在区间内返回间隔均匀的值。</p><ul><li><code>start</code>：序列的起始值。</li><li><code>stop</code>：序列的结束值。</li><li><code>num</code>：生成的样本数。默认值为50。</li><li><code>endpoint</code>：布尔值，如果为真，则最后一个样本包含在序列内，默认为True。</li><li><code>retstep</code>：布尔值，如果为真，返回间距。</li><li><code>dtype</code>：数组的类型。</li></ul></blockquote><h3 id="ones方法创建"><a href="#ones方法创建" class="headerlink" title="ones方法创建"></a>ones方法创建</h3><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python">numpy.ones(shape, dtype=<span class="hljs-literal">None</span>, order=<span class="hljs-string">&#x27;C&#x27;</span>)<br>numpy.ones_like(a, dtype)<br>numpy.ones((<span class="hljs-number">2</span>, <span class="hljs-number">3</span>))<br></code></pre></td></tr></table></figure><blockquote><p>Numpy.ones用于快速创建数值全部为1的多维数组</p><ul><li><code>shape</code>：用于指定数组形状，例如（1， 2）或 3。</li><li><code>dtype</code>：数据类型。</li><li><code>order</code>：<code>&#123;&#39;C&#39;，&#39;F&#39;&#125;</code>，按行或列方式储存数组。</li></ul></blockquote><h3 id="zeros方法创建"><a href="#zeros方法创建" class="headerlink" title="zeros方法创建"></a>zeros方法创建</h3><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs python">numpy.zeros(shape, dtype=<span class="hljs-literal">None</span>, order=<span class="hljs-string">&#x27;C&#x27;</span>)<br>numpy.zeros_like(a, dtype)<br>例：np.zeros((<span class="hljs-number">3</span>, <span class="hljs-number">2</span>))<br></code></pre></td></tr></table></figure><blockquote><p>和ones方法相似，只是全部填充为0</p><ul><li><code>shape</code>：用于指定数组形状，例如<code>（1， 2）</code>或<code>3</code>。</li><li><code>dtype</code>：数据类型。</li><li><code>order</code>：<code>&#123;&#39;C&#39;，&#39;F&#39;&#125;</code>，按行或列方式储存数组。</li></ul></blockquote><h3 id="eye方法创建"><a href="#eye方法创建" class="headerlink" title="eye方法创建"></a>eye方法创建</h3><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">numpy.eye(N, M=<span class="hljs-literal">None</span>, k=<span class="hljs-number">0</span>, dtype=&lt;<span class="hljs-built_in">type</span> <span class="hljs-string">&#x27;float&#x27;</span>&gt;)<br>np.eye(<span class="hljs-number">5</span>, <span class="hljs-number">4</span>, <span class="hljs-number">3</span>)<br></code></pre></td></tr></table></figure><blockquote><p>用于创建一个二维数组，特点是k对角线上的值为1，其余值全部为0</p><ul><li><code>N</code>：输出数组的行数。</li><li><code>M</code>：输出数组的列数。</li><li><code>k</code>：对角线索引：0（默认）是指主对角线，正值是指上对角线，负值是指下对角线。</li></ul></blockquote><h2 id="从数据文件、函数中创建ndarray"><a href="#从数据文件、函数中创建ndarray" class="headerlink" title="从数据文件、函数中创建ndarray"></a>从数据文件、函数中创建ndarray</h2><p>Numpy提供了下面5个方法：</p><blockquote><ul><li><code>frombuffer（buffer）</code>：将缓冲区转换为 <code>1</code> 维数组。</li><li><code>fromfile（file，dtype，count，sep）</code>：从文本或二进制文件中构建多维数组。</li><li><code>fromfunction（function，shape）</code>：通过函数返回值来创建多维数组。</li><li><code>fromiter（iterable，dtype，count）</code>：从可迭代对象创建 <code>1</code> 维数组。</li><li><code>fromstring（string，dtype，count，sep）</code>：从字符串中创建 <code>1</code> 维数组。</li></ul></blockquote><h1 id="数组属性"><a href="#数组属性" class="headerlink" title="数组属性"></a>数组属性</h1><blockquote><ol><li><code>ndarray.T</code> 用于数组的转置，与 <code>.transpose()</code> 相同。</li><li><code>ndarray.dtype</code> 用来输出数组包含元素的数据类型。</li><li><code>ndarray.real</code>用来输出数组包含元素的实部。</li><li><code>ndarray.imag</code> 用来输出数组包含元素的虚部。</li><li><code>ndarray.size</code>用来输出数组中的总包含元素数。</li><li><code>ndarray.itemsize</code>输出一个数组元素的字节数。</li><li><code>ndarray.nbytes</code>用来输出数组的元素总字节数。</li><li><code>ndarray.ndim</code>用来输出数组维度。</li><li><code>ndarray.shape</code>用来输出数组形状。</li><li><code>ndarray.strides</code>用来遍历数组时，输出每个维度中步进的字节数组。</li></ol></blockquote><h1 id="数组维度和形状"><a href="#数组维度和形状" class="headerlink" title="数组维度和形状"></a>数组维度和形状</h1><h2 id="数组基本操作"><a href="#数组基本操作" class="headerlink" title="数组基本操作"></a>数组基本操作</h2><h3 id="重设形状"><a href="#重设形状" class="headerlink" title="重设形状"></a>重设形状</h3><blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">numpy.reshape(a, newshape)<br></code></pre></td></tr></table></figure><p><code>reshape</code>可以在不改变数组数据的同时改变数组形状，其中</p><p><code>numpy.reshape()</code>等效于<code>ndarray.reshape()</code></p><p><code>a</code>表示原数组，<code>newshape</code>表示用于指定新的形状（整数或者数组）</p></blockquote><h3 id="数组展开"><a href="#数组展开" class="headerlink" title="数组展开"></a>数组展开</h3><blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs plain">numpy.ravel(a, order&#x3D;&#39;C&#39;)<br>ndarray.flatten()<br></code></pre></td></tr></table></figure><p><code>ravel</code>的目的是将任意形状的数组扁平化，变为1维数组</p><p>a代表需要处理的数组，<code>order</code>表示变换时读取的顺序，默认按行依次读取，当<code>order=&#39;F&#39;</code>时按列读取</p></blockquote><h3 id="轴移动"><a href="#轴移动" class="headerlink" title="轴移动"></a>轴移动</h3><blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">numpy.moveaxis(a, source, destination)<br></code></pre></td></tr></table></figure><p><code>moveaxis</code>可以将数组的轴移动到新的位置</p><ul><li><code>a</code>：数组。</li><li><code>source</code>：要移动的轴的原始位置。（元组下标）</li><li><code>destination</code>：要移动的轴的目标位置。（元组下标）</li></ul></blockquote><h3 id="轴交换"><a href="#轴交换" class="headerlink" title="轴交换"></a>轴交换</h3><blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">numpy.swapaxes(a, axis1, axis2)<br></code></pre></td></tr></table></figure><p><code>swapaxes</code>可以用来交换数组的轴</p><ul><li><code>a</code>：数组。</li><li><code>axis1</code>：需要交换的轴 1 位置。</li><li><code>axis2</code>：需要与轴 1 交换位置的轴 1 位置。</li></ul></blockquote><h3 id="数组去重"><a href="#数组去重" class="headerlink" title="数组去重"></a>数组去重</h3><blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">np.unique(a)<br></code></pre></td></tr></table></figure><p><strong>去重数组元素中重复的元素</strong></p></blockquote><h3 id="数组转置"><a href="#数组转置" class="headerlink" title="数组转置"></a>数组转置</h3><blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">numpy.transpose(a, axes&#x3D;None)<br></code></pre></td></tr></table></figure><p><code>transpose</code>类似于矩阵的转置，可以将二维数组的横轴和纵轴交换</p><ul><li><code>a</code>：数组。</li><li><code>axis</code>：该值默认为 <code>none</code>，表示转置。如果有值，那么则按照值替换轴。</li></ul></blockquote><h3 id="维度改变"><a href="#维度改变" class="headerlink" title="维度改变"></a>维度改变</h3><blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs plain">numpy.atleast_1d()<br>numpy.atleast_2d()<br>numpy.atleast_3d()<br></code></pre></td></tr></table></figure><p><code>atleast_xd</code>支持输入数据直接视为<code>x</code>维，<code>x</code>可以表示：<code>1，2，3</code></p></blockquote><h3 id="类型转换"><a href="#类型转换" class="headerlink" title="类型转换"></a>类型转换</h3><blockquote><p>在Numpy中，还有一系列以as开头的方法，可以将特定输入转化为数组，亦可将数组转换为矩阵、标量、ndarray等</p><ul><li><code>asarray(a，dtype，order)</code>：将特定输入转换为数组。</li><li><code>asanyarray(a，dtype，order)</code>：将特定输入转换为 <code>ndarray</code>。</li><li><code>asmatrix(data，dtype)</code>：将特定输入转换为矩阵。</li><li><code>asfarray(a，dtype)</code>：将特定输入转换为 <code>float</code> 类型的数组。</li><li><code>asarray_chkfinite(a，dtype，order)</code>：将特定输入转换为数组，检查 <code>NaN</code> 或 <code>infs</code>。</li><li><code>asscalar(a)</code>：将大小为 1 的数组转换为标量。</li></ul></blockquote><h3 id="数组连接"><a href="#数组连接" class="headerlink" title="数组连接"></a>数组连接</h3><blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">numpy.concatenate((a1, a2, ...), axis&#x3D;0)<br></code></pre></td></tr></table></figure><p><code>concatenate</code>可以将多个数组沿指定轴连接在一起</p><ul><li><code>(a1, a2, ...)</code>：需要连接的数组。</li><li><code>axis</code>：指定连接轴。为1表示沿着横轴连接</li></ul></blockquote><h3 id="数组堆叠"><a href="#数组堆叠" class="headerlink" title="数组堆叠"></a>数组堆叠</h3><blockquote><p>在 NumPy 中，以下方法可用于数组的堆叠：</p><ul><li><code>stack(arrays，axis)</code>：沿着新轴连接数组的序列。</li><li><code>column_stack()</code>：将 1 维数组作为列堆叠到 2 维数组中。</li><li><code>hstack()</code>：按水平方向堆叠数组。</li><li><code>vstack()</code>：按垂直方向堆叠数组。</li><li><code>dstack()</code>：按深度方向堆叠数组。</li></ul></blockquote><h3 id="数组拆分"><a href="#数组拆分" class="headerlink" title="数组拆分"></a>数组拆分</h3><blockquote><p><code>split</code> 及与之相似的一系列方法主要是用于数组的拆分，列举如下：</p><ul><li><code>split(ary，indices_or_sections，axis)</code>：将数组拆分为多个子数组。</li><li><code>dsplit(ary，indices_or_sections)</code>：按深度方向将数组拆分成多个子数组。</li><li><code>hsplit(ary，indices_or_sections)</code>：按水平方向将数组拆分成多个子数组。</li><li><code>vsplit(ary，indices_or_sections)</code>：按垂直方向将数组拆分成多个子数组。</li></ul></blockquote><h3 id="增删改"><a href="#增删改" class="headerlink" title="增删改"></a>增删改</h3><blockquote><ul><li><code>delete(arr，obj，axis)</code>：沿特定轴删除数组中的子数组。</li><li><code>insert(arr，obj，values，axis)</code>：依据索引在特定轴之前插入值。</li><li><code>append(arr，values，axis)</code>：将值附加到数组的末尾，并返回 1 维数组。</li><li><code>resize(a，new_shape)</code>：对数组尺寸进行重新设定。</li><li><code>fliplr(m)</code>：左右翻转数组。</li><li><code>flipud(m)</code>：上下翻转数组。</li></ul></blockquote><h2 id="Numpy随机数"><a href="#Numpy随机数" class="headerlink" title="Numpy随机数"></a>Numpy随机数</h2><blockquote><ol><li><code>numpy.random.rand(d0, d1, ..., dn)</code> 方法的作用为：指定一个数组，并使用 <code>[0, 1)</code> 区间随机数据填充，这些数据均匀分布。</li><li><code>numpy.random.randn(d0, d1, ..., dn)</code> 与 <code>numpy.random.rand(d0, d1, ..., dn)</code> 的区别在于，前者是从标准正态分布中返回一个或多个样本值。</li><li><code>randint(low, high, size, dtype)</code> 方法将会生成 <code>[low, high)</code> 的随机整数。注意这是一个半开半闭区间。</li><li><code>random_sample(size)</code> 方法将会在 <code>[0, 1)</code> 区间内生成指定 <code>size</code> 的随机浮点数。</li><li>与 <code>numpy.random.random_sample</code> 类似的方法还有：<ul><li><code>numpy.random.random([size])</code></li><li><code>numpy.random.ranf([size])</code></li><li><code>numpy.random.sample([size])</code></li></ul></li><li><code>choice(a, size, replace, p)</code> 方法将会给定的数组里随机抽取几个值，该方法类似于随机抽样。</li></ol></blockquote><h2 id="概率密度分布"><a href="#概率密度分布" class="headerlink" title="概率密度分布"></a>概率密度分布</h2><blockquote><ul><li><code>numpy.random.beta(a，b，size)</code>：从 Beta 分布中生成随机数。</li><li><code>numpy.random.binomial(n, p, size)</code>：从二项分布中生成随机数。</li><li><code>numpy.random.chisquare(df，size)</code>：从卡方分布中生成随机数。</li><li><code>numpy.random.dirichlet(alpha，size)</code>：从 Dirichlet 分布中生成随机数。</li><li><code>numpy.random.exponential(scale，size)</code>：从指数分布中生成随机数。</li><li><code>numpy.random.f(dfnum，dfden，size)</code>：从 F 分布中生成随机数。</li><li><code>numpy.random.gamma(shape，scale，size)</code>：从 Gamma 分布中生成随机数。</li><li><code>numpy.random.geometric(p，size)</code>：从几何分布中生成随机数。</li><li><code>numpy.random.gumbel(loc，scale，size)</code>：从 Gumbel 分布中生成随机数。</li><li><code>numpy.random.hypergeometric(ngood, nbad, nsample, size)</code>：从超几何分布中生成随机数。</li><li><code>numpy.random.laplace(loc，scale，size)</code>：从拉普拉斯双指数分布中生成随机数。</li><li><code>numpy.random.logistic(loc，scale，size)</code>：从逻辑分布中生成随机数。</li><li><code>numpy.random.lognormal(mean，sigma，size)</code>：从对数正态分布中生成随机数。</li><li><code>numpy.random.logseries(p，size)</code>：从对数系列分布中生成随机数。</li><li><code>numpy.random.multinomial(n，pvals，size)</code>：从多项分布中生成随机数。</li><li><code>numpy.random.multivariate_normal(mean, cov, size)</code>：从多变量正态分布绘制随机样本。</li><li><code>numpy.random.negative_binomial(n, p, size)</code>：从负二项分布中生成随机数。</li><li><code>numpy.random.noncentral_chisquare(df，nonc，size)</code>：从非中心卡方分布中生成随机数。</li><li><code>numpy.random.noncentral_f(dfnum, dfden, nonc, size)</code>：从非中心 F 分布中抽取样本。</li><li><code>numpy.random.normal(loc，scale，size)</code>：==从正态分布绘制随机样本。参数，均值和偏差==</li><li><code>numpy.random.pareto(a，size)</code>：从具有指定形状的 Pareto II 或 Lomax 分布中生成随机数。</li><li><code>numpy.random.poisson(lam，size)</code>：从泊松分布中生成随机数。</li><li><code>numpy.random.power(a，size)</code>：从具有正指数 a-1 的功率分布中在 0，1 中生成随机数。</li><li><code>numpy.random.randn(d0,d1,....dn)</code>:从标准正态分布中返回一个或多个样本值</li><li><code>numpy.random.randint(low, high=None, size=None, dtype=&#39;l&#39;)</code>:从一个从一个均匀分布中随机采样，生成一个整数或N维整数数组</li><li><code>numpy.random.rayleigh(scale，size)</code>：从瑞利分布中生成随机数。</li><li><code>numpy.random.standard_cauchy(size)</code>：从标准 Cauchy 分布中生成随机数。</li><li><code>numpy.random.standard_exponential(size)</code>：从标准指数分布中生成随机数。</li><li><code>numpy.random.standard_gamma(shape，size)</code>：从标准 Gamma 分布中生成随机数。</li><li><code>numpy.random.standard_normal(size)</code>：从标准正态分布中生成随机数。</li><li><code>numpy.random.standard_t(df，size)</code>：从具有 df 自由度的标准学生 t 分布中生成随机数。</li><li><code>numpy.random.triangular(left，mode，right，size)</code>：从三角分布中生成随机数。</li><li><code>numpy.random.uniform(low=0，high=1，size)</code>：从均匀分布中生成随机数。</li><li><code>numpy.random.vonmises(mu，kappa，size)</code>：从 von Mises 分布中生成随机数。</li><li><code>numpy.random.wald(mean，scale，size)</code>：从 Wald 或反高斯分布中生成随机数。</li><li><code>numpy.random.weibull(a，size)</code>：从威布尔分布中生成随机数。</li><li><code>numpy.random.zipf(a，size)</code>：从 Zipf 分布中生成随机数。</li></ul></blockquote><h1 id="数学函数"><a href="#数学函数" class="headerlink" title="数学函数"></a>数学函数</h1><h2 id="三角函数"><a href="#三角函数" class="headerlink" title="三角函数"></a>三角函数</h2><blockquote><ul><li><code>numpy.sin(x)</code>：三角正弦。</li><li><code>numpy.cos(x)</code>：三角余弦。</li><li><code>numpy.tan(x)</code>：三角正切。</li><li><code>numpy.arcsin(x)</code>：三角反正弦。</li><li><code>numpy.arccos(x)</code>：三角反余弦。</li><li><code>numpy.arctan(x)</code>：三角反正切。</li><li><code>numpy.hypot(x1,x2)</code>：直角三角形求斜边。</li><li><code>numpy.degrees(x)</code>：弧度转换为度。</li><li><code>numpy.radians(x)</code>：度转换为弧度。</li><li><code>numpy.deg2rad(x)</code>：度转换为弧度。</li><li><code>numpy.rad2deg(x)</code>：弧度转换为度。</li></ul></blockquote><h2 id="双曲线函数"><a href="#双曲线函数" class="headerlink" title="双曲线函数"></a>双曲线函数</h2><blockquote><ul><li><code>numpy.sinh(x)</code>：双曲正弦。</li><li><code>numpy.cosh(x)</code>：双曲余弦。</li><li><code>numpy.tanh(x)</code>：双曲正切。</li><li><code>numpy.arcsinh(x)</code>：反双曲正弦。</li><li><code>numpy.arccosh(x)</code>：反双曲余弦。</li><li><code>numpy.arctanh(x)</code>：反双曲正切。</li></ul></blockquote><h2 id="数值修约"><a href="#数值修约" class="headerlink" title="数值修约"></a>数值修约</h2><blockquote><ul><li><code>numpy.around(a)</code>：平均到给定的小数位数。</li><li><code>numpy.round_(a)</code>：将数组舍入到给定的小数位数。</li><li><code>numpy.rint(x)</code>：修约到最接近的整数。</li><li><code>numpy.fix(x, y)</code>：向 0 舍入到最接近的整数。</li><li><code>numpy.floor(x)</code>：返回输入的底部(标量 x 的底部是最大的整数 i)。</li><li><code>numpy.ceil(x)</code>：返回输入的上限(标量 x 的底部是最小的整数 i).</li><li><code>numpy.trunc(x)</code>：返回输入的截断值。</li></ul></blockquote><h2 id="求和、求积、差分"><a href="#求和、求积、差分" class="headerlink" title="求和、求积、差分"></a>求和、求积、差分</h2><blockquote><ul><li><code>numpy.prod(a, axis, dtype, keepdims)</code>：返回指定轴上的数组元素的乘积。</li><li><code>numpy.sum(a, axis, dtype, keepdims)</code>：返回指定轴上的数组元素的总和。</li><li><code>numpy.nanprod(a, axis, dtype, keepdims)</code>：返回指定轴上的数组元素的乘积, 将 NaN 视作 1。</li><li><code>numpy.nansum(a, axis, dtype, keepdims)</code>：返回指定轴上的数组元素的总和, 将 NaN 视作 0。</li><li><code>numpy.cumprod(a, axis, dtype)</code>：返回沿给定轴的元素的累积乘积。</li><li><code>numpy.cumsum(a, axis, dtype)</code>：返回沿给定轴的元素的累积总和。</li><li><code>numpy.nancumprod(a, axis, dtype)</code>：返回沿给定轴的元素的累积乘积, 将 NaN 视作 1。</li><li><code>numpy.nancumsum(a, axis, dtype)</code>：返回沿给定轴的元素的累积总和, 将 NaN 视作 0。</li><li><code>numpy.diff(a, n, axis)</code>：计算沿指定轴的第 n 个离散差分。</li><li><code>numpy.ediff1d(ary, to_end, to_begin)</code>：数组的连续元素之间的差异。</li><li><code>numpy.gradient(f)</code>：返回 N 维数组的梯度。</li><li><code>numpy.cross(a, b, axisa, axisb, axisc, axis)</code>：返回两个(数组）向量的叉积。</li><li><code>numpy.trapz(y, x, dx, axis)</code>：使用复合梯形规则沿给定轴积分。</li></ul></blockquote><h2 id="指数和对数"><a href="#指数和对数" class="headerlink" title="指数和对数"></a>指数和对数</h2><blockquote><ul><li><code>numpy.exp(x)</code>：计算输入数组中所有元素的指数。</li><li><code>numpy.log(x)</code>：计算自然对数。</li><li><code>numpy.log10(x)</code>：计算常用对数。</li><li><code>numpy.log2(x)</code>：计算二进制对数。</li></ul></blockquote><h2 id="算数运算"><a href="#算数运算" class="headerlink" title="算数运算"></a>算数运算</h2><blockquote><ul><li><code>numpy.add(x1, x2)</code>：对应元素相加。</li><li><code>numpy.reciprocal(x)</code>：求倒数 1/x。</li><li><code>numpy.negative(x)</code>：求对应负数。</li><li><code>numpy.multiply(x1, x2)</code>：求解乘法。</li><li><code>numpy.divide(x1, x2)</code>：相除 x1/x2。</li><li><code>numpy.power(x1, x2)</code>：类似于 x1^x2。</li><li><code>numpy.subtract(x1, x2)</code>：减法。</li><li><code>numpy.fmod(x1, x2)</code>：返回除法的元素余项。</li><li><code>numpy.mod(x1, x2)</code>：返回余项。</li><li><code>numpy.modf(x1)</code>：返回数组的小数和整数部分。</li><li><code>numpy.remainder(x1, x2)</code>：返回除法余数</li></ul></blockquote><h2 id="逻辑运算和判断函数"><a href="#逻辑运算和判断函数" class="headerlink" title="逻辑运算和判断函数"></a>逻辑运算和判断函数</h2><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs python">a[条件] = <span class="hljs-number">1</span> <span class="hljs-comment"># 将满足条件的数组元素设置为1</span><br>np.<span class="hljs-built_in">all</span>(条件) <span class="hljs-comment"># 查询是否都满足条件</span><br>numpy.<span class="hljs-built_in">any</span>(条件) <span class="hljs-comment"># 查询是否有满足条件的</span><br>numpy.where(条件，<span class="hljs-number">1</span>，<span class="hljs-number">0</span>) <span class="hljs-comment"># 三目运算，符合条件的设置为1，不符合的为0</span><br></code></pre></td></tr></table></figure></blockquote><h2 id="统计运算"><a href="#统计运算" class="headerlink" title="统计运算"></a>统计运算</h2><blockquote><ul><li><code>np.max()</code> 沿指定轴计算中间值</li><li><code>np.min() </code>沿指定轴计算中间值</li><li><code>np.median()</code> 沿指定轴计算中间值</li><li><code>np.mean() </code>沿指定轴计算平均数</li><li><code>np.std() </code>沿指定轴计算标准偏差</li><li><code>np.var() </code>沿指定轴计算方差</li><li><code>np.argmax(axis=) </code>最大元素对应的下标</li><li><code>np.argmin(axis=)</code> 最小元素对应的下标</li></ul></blockquote><h2 id="矩阵和向量积"><a href="#矩阵和向量积" class="headerlink" title="矩阵和向量积"></a>矩阵和向量积</h2><blockquote><ul><li><code>numpy.dot(a, b)</code>：求解两个数组的点积。</li><li><code>numpy.vdot(a, b)</code>：求解两个向量的点积。</li><li><code>numpy.inner(a, b)</code>：求解两个数组的内积。</li><li><code>numpy.outer(a, b)</code>：求解两个向量的外积。</li><li><code>numpy.matmul(a, b)</code>：求解两个数组的矩阵乘积。</li><li><code>numpy.tensordot(a, b)</code>：求解张量点积。</li><li><code>numpy.kron(a, b)</code>：计算 Kronecker 乘积。</li></ul></blockquote><h2 id="代数运算"><a href="#代数运算" class="headerlink" title="代数运算"></a>代数运算</h2><blockquote><ul><li><code>numpy.linalg.cholesky(a)</code>：Cholesky 分解。</li><li><code>numpy.linalg.qr(a ,mode)</code>：计算矩阵的 QR 因式分解。</li><li><code>numpy.linalg.svd(a ,full_matrices,compute_uv)</code>：奇异值分解。</li><li><code>numpy.linalg.eig(a)</code>：计算正方形数组的特征值和右特征向量。</li><li><code>numpy.linalg.eigh(a, UPLO)</code>：返回 Hermitian 或对称矩阵的特征值和特征向量。</li><li><code>numpy.linalg.eigvals(a)</code>：计算矩阵的特征值。</li><li><code>numpy.linalg.eigvalsh(a, UPLO)</code>：计算 Hermitian 或真实对称矩阵的特征值。</li><li><code>numpy.linalg.norm(x ,ord,axis,keepdims)</code>：计算矩阵或向量范数。</li><li><code>numpy.linalg.cond(x ,p)</code>：计算矩阵的条件数。</li><li><code>numpy.linalg.det(a)</code>：计算数组的行列式。</li><li><code>numpy.linalg.matrix_rank(M ,tol)</code>：使用奇异值分解方法返回秩。</li><li><code>numpy.linalg.slogdet(a)</code>：计算数组的行列式的符号和自然对数。</li><li><code>numpy.trace(a ,offset,axis1,axis2,dtype,out)</code>：沿数组的对角线返回总和。</li><li><code>numpy.linalg.solve(a, b)</code>：求解线性矩阵方程或线性标量方程组。</li><li><code>numpy.linalg.tensorsolve(a, b ,axes)</code>：为 x 解出张量方程 a x = b</li><li><code>numpy.linalg.lstsq(a, b ,rcond)</code>：将最小二乘解返回到线性矩阵方程。</li><li><code>numpy.linalg.inv(a)</code>：计算逆矩阵。</li><li><code>numpy.linalg.pinv(a ,rcond)</code>：计算矩阵的（Moore-Penrose）伪逆。</li><li><code>numpy.linalg.tensorinv(a ,ind)</code>：计算 N 维数组的逆。</li></ul></blockquote><h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><blockquote><ul><li><code>numpy.angle(z, deg)</code>：返回复参数的角度。</li><li><code>numpy.real(val)</code>：返回数组元素的实部。</li><li><code>numpy.imag(val)</code>：返回数组元素的虚部。</li><li><code>numpy.conj(x)</code>：按元素方式返回共轭复数。</li><li><code>numpy.convolve(a, v, mode)</code>：返回线性卷积。</li><li><code>numpy.sqrt(x)</code>：平方根。</li><li><code>numpy.cbrt(x)</code>：立方根。</li><li><code>numpy.square(x)</code>：平方。</li><li><code>numpy.absolute(x)</code>：绝对值, 可求解复数。</li><li><code>numpy.fabs(x)</code>：绝对值。</li><li><code>numpy.sign(x)</code>：符号函数。</li><li><code>numpy.maximum(x1, x2)</code>：最大值。</li><li><code>numpy.minimum(x1, x2)</code>：最小值。</li><li><code>numpy.nan_to_num(x)</code>：用 0 替换 NaN。</li><li><code>numpy.interp(x, xp, fp, left, right, period)</code>：线性插值。</li></ul></blockquote><h1 id="数组索引和切片"><a href="#数组索引和切片" class="headerlink" title="数组索引和切片"></a>数组索引和切片</h1><h2 id="数据索引"><a href="#数据索引" class="headerlink" title="数据索引"></a>数据索引</h2><blockquote><ol><li><p>一维数组索引：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs plain">a&#x3D;np.arange(10)<br>a[1] #获取索引值为1的数据<br>a[[1,2,3]] #分别获取索引值1，2，3的数据<br></code></pre></td></tr></table></figure></li><li><p>二维数组索引：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs plain">a &#x3D; np.arange(20).reshape(4, 5)<br>a[[1,2],[3,4]] # 获取第2行，第3列的数据<br></code></pre></td></tr></table></figure></li><li><p>三维数组索引：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs python">a = np.arange(<span class="hljs-number">30</span>).reshape(<span class="hljs-number">2</span>, <span class="hljs-number">5</span>, <span class="hljs-number">3</span>)<br>a[[<span class="hljs-number">0</span>, <span class="hljs-number">1</span>], [<span class="hljs-number">1</span>, <span class="hljs-number">2</span>], [<span class="hljs-number">1</span>, <span class="hljs-number">2</span>]]<br></code></pre></td></tr></table></figure></li></ol></blockquote><h2 id="数组切片"><a href="#数组切片" class="headerlink" title="数组切片"></a>数组切片</h2><blockquote><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs plain">Ndarray[start:stop:step]<br></code></pre></td></tr></table></figure><p><code>[start:stop:step]</code> 分别代表 <code>[起始索引:截至索引:步长]</code>。</p><p>一维数组：a[5:10]</p><p>二维数组：[0:3, 2:4]</p></blockquote><h2 id="排序、搜索、计数"><a href="#排序、搜索、计数" class="headerlink" title="排序、搜索、计数"></a>排序、搜索、计数</h2><h3 id="排序"><a href="#排序" class="headerlink" title="排序"></a>排序</h3><blockquote><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs python">numpy.sort(a, axis=-<span class="hljs-number">1</span>, kind=<span class="hljs-string">&#x27;quicksort&#x27;</span>, order=<span class="hljs-literal">None</span>)<br></code></pre></td></tr></table></figure><ul><li><code>a</code>：数组。</li><li><code>axis</code>：要排序的轴。如果为<code>None</code>，则在排序之前将数组铺平。默认值为 <code>-1</code>，沿最后一个轴排序。</li><li><code>kind</code>：<code>&#123;&#39;quicksort&#39;，&#39;mergesort&#39;，&#39;heapsort&#39;&#125;</code>，排序算法。默认值为 <code>quicksort</code>。</li></ul><p>其他排序方法：</p><ul><li><code>numpy.lexsort(keys ,axis)</code>：使用多个键进行间接排序。</li><li><code>numpy.argsort(a ,axis,kind,order)</code>：沿给定轴执行间接排序。</li><li><code>numpy.msort(a)</code>：沿第 1 个轴排序。</li><li><code>numpy.sort_complex(a)</code>：针对复数排序。</li></ul></blockquote><h3 id="搜索和计数"><a href="#搜索和计数" class="headerlink" title="搜索和计数"></a>搜索和计数</h3><blockquote><ul><li><code>argmax(a ,axis,out)</code>：返回数组中指定轴的最大值的索引。</li><li><code>nanargmax(a ,axis)</code>：返回数组中指定轴的最大值的索引,忽略 NaN。</li><li><code>argmin(a ,axis,out)</code>：返回数组中指定轴的最小值的索引。</li><li><code>nanargmin(a ,axis)</code>：返回数组中指定轴的最小值的索引,忽略 NaN。</li><li><code>argwhere(a)</code>：返回数组中非 0 元素的索引,按元素分组。</li><li><code>nonzero(a)</code>：返回数组中非 0 元素的索引。</li><li><code>flatnonzero(a)</code>：返回数组中非 0 元素的索引,并铺平。</li><li><code>where(条件,x,y)</code>：根据指定条件,从指定行、列返回元素。</li><li><code>searchsorted(a,v ,side,sorter)</code>：查找要插入元素以维持顺序的索引。</li><li><code>extract(condition,arr)</code>：返回满足某些条件的数组的元素。</li><li><code>count_nonzero(a)</code>：计算数组中非 0 元素的数量。</li></ul></blockquote>]]></content>
      
      
      
    </entry>
    
    
  
  
</search>
